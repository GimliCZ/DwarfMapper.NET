bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/
# Verify snapshot received files (temporary; only .verified.txt is committed)
*.received.txt
# JetBrains Rider IDE
.idea/
# Coverage / test output (coverlet XPlat Code Coverage drops these)
TestResults/
**/TestResults/
coverage.cobertura.xml

# BenchmarkDotNet output
**/BenchmarkDotNet.Artifacts/

# Signing material — NEVER commit private keys / PFX / base64-encoded certs
*.pfx
*.snk
*.p12
*.pem
*.b64
# Release/pack output
artifacts/
*.nupkg
*.snupkg
# SBOM output
/sbom/
**/bom.xml
**/bom.json

# project-rag / FastEmbed model cache — dropped in cwd when the RAG MCP server runs here; never commit it.
.fastembed_cache/
