<!-- SPDX-License-Identifier: GPL-2.0-only -->

# The task list

**Standing rule, adopted 2026-08-16: every issue found goes in here, as a task, when it is found.**
Not into a ledger line, not into a report addendum, not into a category table. Here. One store.

This file supersedes the issue-tracking role of `CARRY-FORWARD.md` (which stays as the written-up *reasoning*
behind these items — the detail is worth keeping, it just should not be where work is tracked).

**Status:** `TODO` · `WIP` · `DONE` · `DECIDE` (blocked on a maintainer ruling, not on work) ·
`DROP` (considered and rejected — kept so nobody re-raises it).

**Where the detail lives:** `CF §n` = `Issues/round20/CARRY-FORWARD.md` section n · `Dn` = an entry in
`tests/DwarfMapper.Generator.Tests/Contracts/DeclaredDivergences.cs` · `R21` =
`Issues/round21/RESEARCH.md` · ledger = `.superpowers/sdd/2026-08-16-round20-generator-defects/progress.md`
(git-ignored; rulings and per-task history live there).

---

## NOW — state of the work

Everything below this line is a backlog. This section is what is *actually happening*, so it can be read
without asking.

**Branch:** `feat/round22-gates`, worktree `C:/Users/Jouda/RiderProjects/DwarfMapper-r22`. Nothing pushed.
Whole solution builds 0 warnings / 0 errors with samples.
*(Corrected 2026-08-22, W6. This block described the pre-merge world: `feat/surface-coverage-architecture`
in the `DwarfMapper-surface` worktree, F1 and D-e blocked on a human. All three are closed — see the rows.
The sections BELOW this one are history and are not rewritten; this one is a status board and is.)*

**THE A-TRACK IS CLOSED.** Every round-20 generator task, A0 through A14, is complete and reviewed. **The
surface matrix is GREEN: 866 passed, 0 failed** (re-measured 2026-08-22 on `feat/round22-gates`). Every
product divergence round 20 opened is settled — fixed, refused with a diagnostic, or reclassified as
structural with the measurement in the commit that did it.

| | |
|---|---|
| **In flight** | **Round 22** — the 97 % gates program and the compiler-testing arc, per `docs/superpowers/plans/2026-08-21-round22-compiler-testing-and-97-gates.md`. Layers 1–4 are landing on `feat/round22-gates`; the round-20 rows below are being closed as that work reaches them. |
| **Next** | **W7** (H3, Meziantou phase 2). Layer 0 is maintainer-gated on the master/`ci.yml` push. |
| **Blocked on a human** | **D-a**, **D-c**, **D-f**, **F3**, **H2**, **H8** and the generator dead-code rulings — all listed in the plan's maintainer-only section. F1 and D-e are DONE. |

**Live ceilings, read from the code 2026-08-22:** findings **2** · declared cells **4** · `NotCompilable`
**0** · `EmittedInvalidCode` **0** · `NoSuchSite` **48 + 68** (exact per-cause pins,
`registry-has-no-mapper-class` and `no-mapping-method`, replacing the old total of 116 — B6) ·
`StructurallyExcused` **13** · `UnhonouredButLoud` **14** · `Unaskable` **44**. From **23 findings / 162
cells** when round 20 began.

`EmittedInvalidCode` — *the generator emitted code that does not compile*, a NEW population A12 introduced
and a genuinely different thing from `NotCompilable` — stood at **10** when the surface branch merged
(`dc385d4`) and reads **0** now. Round 21's T6/B27 drained it: `bda57e0` refuses a `[GenerateMap]` that
duplicates an existing map signature as `DWARF094`, taking the population 8 → 0, and `d131c76` merged that.
Both populations are exact pins at zero now, so neither can refill quietly. (An earlier version of the line
above confused the two populations; this one had gone on quoting the 10 for a round after it was paid off.)

The two findings that remain are **`NullCollections`@`Projection`** (a ruled design decision) and
**`MaxDepth`@`Span`/`AsyncStream`** (a tighter bound ignored; the default 64 still applies). Both are
maintainer-acknowledged, neither is a silent defect.

**Layers 0 and 1 are complete.** Seven new diagnostics (`DWARF087`–`DWARF093`) plus `DWARFR10` and `DWARFR11`,
eleven structural unifications, and three product defects found that no test or sample reached: a shipped
stack overflow (`[AfterMap]` recursing into `Update`), duplicate `[FlattenGraph]` emitting uncompilable code,
and `[MapTo]` on a `struct` never having compiled at all. (`DWARF086` is **round 19's**, from its Task 8 — the
range `086`–`093` is eight ids, not seven, and an earlier version of this line miscounted it.)

### Two facts worth knowing before reading anything else here

**Eight round-19 entries had false filed evidence**, each falsified by the task sent to fix it — D2, D8, D10,
D12, D13, D14, D15, D17. Four or more trace to *a fixture too thin to pose the question the entry claimed to
answer*. Round 19 found 23 cells worth investigating, not 23 defects.

**Nine defects this round were "a guard exists on a sibling path and the new code did not inherit it."** Two
shipped generator crashes. The last one (A13) found the notion **three times inline in one file**, and its own
new `TypeFacts.cs` doc comment had warned, in that same commit, that *a predicate correct only for the inputs
that reach it today* is the next version of the defect.

---

## A. Generator defects — the round-20 plan

Fixing any of these turns the build **red** until its `DeclaredDivergences` entry is deleted and the three
ceilings (findings / declared cells / structural — **now 2 / 4 / 13**) are lowered to their newly measured
values. Every row below is `DONE`; the section is kept as the record of what each fix cost and what it
falsified.

| # | Status | Task | Closes |
|---|---|---|---|
| A0 | `DONE` | CHANGELOG must-fixes + `Scan9` (every diagnostic id must be announced) | CF §1.1–1.3 |
| A1 | `DONE` | Duplicate `[FlattenGraph]` destination emitted uncompilable code → `DWARF087` | N4 |
| A2 | `DONE` | The missing arity checks — under review. `DWARFR04` reused for D4 (**no new id**: the brief was wrong that its descriptor was dead; it checks *stacked-attribute* arity, a different thing). `DWARF088` added for D5+D21, one check, two call sites. | D4, D5, D21 |
| A3 | `DONE` | **Closed by A2, unplanned.** D3's cells are `ctor(1)` + a property initializer — named arguments ride on the one-argument constructor — so A2's check fires on them. The narrower alternative was rejected: it would leave `[MapProperty("Id")]` refused and `[MapProperty("Id", Use=…)]` silent. | D3 |
| A4 | `DONE` | Co-located host reads no member-level directives. Fixed: `MapperExtractor` reads them off the host's own members through the one `MemberDirectives` parser the registry already used; the method forms written there are refused as the new **`DWARF089`**. Measured: **2 Honoured, 18 Refused** — the twenty split 6 (the directive ACTS: 2 Honoured + 4 labelled Refused only by a pre-existing `DWARF038`) / 6 (named arguments refused on their merits) / 8 (`DWARF089`). Ceilings 19/113 → **18/93**. Found four issues, filed as **B15–B18**. | D20 |
| A5 | `DONE` | `MapToGenerator` ignored assembly-level config. Fixed **structurally**: the lookup is hoisted into `Pipeline/AssemblyConfiguration`, and all three front doors read it — the class model for its defaults layer, `DwarfGenerator` for `PublicExtensions` (an inline copy until now), and the registry for both. **One change closed two of three.** D19: the by-name wire is refused as the new **`DWARFR10`**, asked with `MapperExtractor.ReadAutoMatchMembers` itself, not a copy. D18: the registry extension class is now `internal` unless the assembly opts in — what `PublicExtensions` always documented as its default; **BREAKING**, in `CHANGELOG.md`. **D17 did NOT close here and its stated reason was measurably false** (A12 later reclassified it as structural on that same measurement) — the `[MapTo]` front door emits an extension class and *no* ambient registry rows, so `RegisterCollectionShapes` has nothing there to withhold; three ways out recorded, none of them A5's to take. Ceilings 15/84 → **13/82**; no other ratchet moved, none raised. Re-measured: exactly 2 of 854 rows differ. | D18, D19 (D17 stands) |
| A6 | `DONE` | `[MapNullSkip]`'s two forms were exact inverses, and **neither was wrong** — both declare `AppliesTo = All` and their docs describe one option at two scopes. The implementation had **three** readers of it: the method endpoints read the method form and never the pair-scoped one, the `[GenerateMap]` and synthesized pairs read the pair-scoped form and had no method, and projection was handed the bare class value and saw neither. Folded into one `MapperExtractor.ResolveNullSkip`, **most-specific-wins** (method → pair → mapper → assembly), which every front door calls; `ResolvePairNullSkip` is gone. **4 cells Silent → Honoured** (the pair-scoped form at CreateMap/UpdateInto), **2 cells Silent → Refused** (`DWARF090` now covers `[MapNullSkip]` element-wise, remedy with the value repeated). **Projection did NOT close and is not deferred by choice:** threading the value there is one line and produces the right behaviour — the same blocking `DWARF028` the class-level option already gets — but its `CS8795` cascade measured all three cells `NotCompilable`, raising `NotCompilableCellCeiling` 99 → **102**. Forbidden, and a closure by relocation. D6/D7 **narrowed** to their Projection cells. `DivergentCellCeiling` 82 → **76**; finding ceiling stays **13**; all five other populations re-measured unchanged. **CLOSED by A12 item 2 (`a7b1799`)**, once A10 removed the `CS8795` cascade that forbade it: the one-line threading landed, the three Projection cells went Silent → Refused, and **D6 and D7 were DELETED**. | D6, D7 |
| A7 | `DONE` | Element-wise endpoints do not inherit method-level directives — **two root causes, not one.** D1+D2 are one shape: the twice-written `DWARF077` check is now a single element-wise gate (`ReportElementWiseDirectiveGaps`) reporting the new **`DWARF090`**, whose message names the pair-scoped remedy (`[MapIgnore<TTarget>]`, `[MapProperty<TSource,TTarget>]`) — measured `Honoured` at those endpoints, so the refusal has a working replacement. **8 cells Silent → Refused.** D16 needed its own fix, the new **`DWARF091`**: `CollectHooks` accepted the partial mapping method as a hook, which at UpdateInto emitted `Update(s, d);` *inside* `Update` — shipped infinite recursion **the matrix scored `Honoured`** (filed as **B19**). **1 cell Silent → Refused, 8 more `NotCompilable` → Refused.** D2's filed evidence was wrong (`DWARF038` is `ImplicitConversionApplied`, not a placement refusal); corrected in its section. Ceilings 18/93 → **15/84**, `NotCompilable` 107 → **99**. | D1, D2, D16 |
| A8 | `DONE` | Projection does not honour member directives — **two directives, two different answers, and one finding whose own evidence was false.** **D10 closes outright** and the record it leaves is a correction: `[Flatten]` was filed as "honoured at CreateMap and UpdateInto" against `nested-pair`, and it was not — that fixture's `Dst` still declares the NESTED member, so the flatten found leaf `X`, matched it to nothing, and emitted **byte-identical output at all five endpoints**; the two `Refused` readings were an incidental `DWARF044` nullable-hop warning. New fixture `flattenable-nested-member` (struct root, destination carries the LEAF, `DWARF001`-by-design baseline) poses the question; `ResolveFlattenInfos` is now one walk both resolvers call, so **projection honours the flatten** (`__s.Child.X`) and refuses an invalid root with the same `DWARF016`, and the element-wise endpoints refuse as **`DWARF090`** naming the dotted `[MapProperty<Src, Dst>("Child.<leaf>","<leaf>")]` remedy — measured `Honoured` there before it was prescribed, because `[Flatten]` has no pair-scoped twin. **D9 narrows to its 4 Projection cells:** 8 element-wise cells close as `DWARF090` (remedy `[MapValue<Dst>("Name","x")]`, measured `Honoured` at both). Projection did not, and was measured rather than argued — `ctor(2)` becomes `Refused (DWARF064)` but the three malformed axes earn `DWARF042`/`DWARF041`, Errors, so `NotCompilableCellCeiling` measured **99 → 102**. Reverted, recorded at the call site, waiting on **A10**. Also corrected in the entry: only `ctor(2)` was ever honoured at Create/Update; the other three are `CS8795` there too. **Object-initializer question, settled with measurement: it reaches NEITHER.** A constant assignment and a leaf pull-up read nothing from the destination, and a threaded directive that measures `Honoured` at Projection is by construction not structurally inapplicable — `StructurallyExcusedCellCeiling` stays **12**. Ceilings 13/76 → **12/62**; `NotCompilable` **99**, `UnhonouredButLoud` **14**, `Unaskable` **44**, `NoSuchSite` **137**, `StructurallyExcused` **12** all re-measured unchanged. One non-ceiling baseline raised deliberately: `SurfaceProbeTests`' fixtures-without-a-member-slot count 17 → **18**, as that assertion's own message instructs. Found two issues, filed as **B25–B26**. **D9 CLOSED by A12 item 3 (`ed75f67`)**: not the one-liner this row predicted — the create-map guard was rebuilt as `TryValidateMapValueTarget`, one statement with two callers, and a sibling `IgnoreObsoleteMembers` gap was swept with it. All four Projection cells closed and **D9 was DELETED**. | D9, D10 |
| A9a | `DONE` | Directives that act at CreateMap and are silent at the other four — **one shape, one gate, three commits.** The new **`DWARF092`** (a Warning, so the cells land as `Refused` and not in A10's population) is reported by `MapperExtractor.ReportCreateMapOnlyDirectives`, ONE function called from the update-into, projection, span-map and async-stream branches, reading through the create-map branch's own readers. **28 cells Silent → Refused.** Two of the three entries had **wrong evidence**: `D8` claimed the directive "acts at CreateMap in BOTH forms" and the OPEN form acted nowhere — the flat pair declares no hierarchy, so the sampled arguments were `typeof(Dst), typeof(Dst)` and the cell was `DWARF035` behind `CS8795`; the new `polymorphic-hierarchy` fixture fixes that and moved one cell OUT of `NotCompilable` (99 → 98). `D13` misstated the mechanism — `[ReverseMap]` does not GENERATE an inverse, it makes a separately-declared one inherit inverted renames, and a missing one is `DWARF052`. `D11`'s evidence held exactly as filed. Element-wise remedies were MEASURED (`d[__i] = Map(s[__i]);` adopts the declared create map) for `[FlattenGraph]` and `[MapDerivedType]`; `[ReverseMap]` deliberately claims no transfer. Ceilings 12/62 → **9/34**, `NotCompilable` 99 → **98**; four others re-measured unchanged. One non-ceiling baseline raised deliberately: fixtures-without-a-member-slot 18 → **19**. | D8, D11, D13 |
| A9b | `DONE` | The rest of A9 — three directives, three commits, **and all three entries had wrong evidence**. `D14`: `[MapCollectionKey]` is `DWARF092`'s MIRROR IMAGE, so that gate is generalized into `ReportDirectivesNotReadHere`, called from **all five** branches with each arm naming its own HOME endpoint (create map for the old trio, update-into for this one) and skipped there; the id is retitled *"Directive is not read at this mapping endpoint"*. Its "acts at UpdateInto" was **false** — the fixture declared `List<Item>` against `List<ItemDto>` and the v1 upsert requires the same element type, so that cell was `DWARF074` behind `CS8795`; fixed, and **`NotCompilable` 98 → 96**. `DWARF074`'s comment claiming a "not an update-into method" arm no call site implemented is corrected in place. `D12`: `[Reinterpret]` joins **`DWARF090`** — the element-wise gate — as its first arm with NO pair-scoped twin, so its remedy is a *declared create map* (measured: `d[__i] = Map(s[__i]);` with `MemoryMarshal.Cast`). Its "acts at Projection" was **false**; that cell is `UnhonouredButLoud` and is left where it is. `D15`: refused as the new **`DWARF093`**, and the fork was decided from the attribute's own contract — it is an *expansion of the `[GenerateMap]` pair list*, and four of five endpoints have no `W<A> -> W<B>` create-map shape to synthesize. Its stated mechanism was **false** too: `DWARF067` is an opinion about the wrapper TYPE, not about placement, and the silence was an early return on an empty pair list. Reported before the shape check, deliberately, so ten cells land as `Refused` and not behind `DWARF067`'s Error. Ceilings 9/34 → **6/12**, `NotCompilable` 98 → **96**; four others re-measured unchanged. Found one issue, filed as **B27**. | D12, D14, D15 |
| A10 | `DONE` | `CS8795` read as `NotCompilable` where it means `Refused` — 96 cells judged for the first time. The probe now asks whether the generator *said something* before it asks whether the compiler accepted the placement, so a `CS8795` accompanied by a blocking DWARF diagnostic reads `Refused`. `NotCompilableCellCeiling` 96 → **14**, and what remained was no longer a placement rule but a residue worth its own verdict — which became A12 item 5. | G4/R4 |
| A11 | `DONE` | No struct / constructor slot in the endpoint templates — 21 cells unmeasurable. All 21 are now measurable; `NoSuchSiteCellCeiling` 137 → **116**, and that population is now wholly structural. The measurement went red on four assertions **by design**, and it surfaced a real product defect rather than absorbing it: `[MapTo]` on a `struct` had never compiled (`CS0037`), filed as **A11-F1** and fixed by **A13**. | G5 |
| A12 | `DONE` | **The five instrument-and-follow-through items, one commit each** (`2645010`, `a7b1799`, `ed75f67`, `f38fbd1`, `874d83d`). (1) `DWARF088` Warning → **Error**; no population moved, and the real work was the sweep — `DWARF088`-as-a-Warning was load-bearing prose in six other places. (2) A6's parked `[MapNullSkip]`@Projection threading; **D6/D7 deleted**. (3) A8's parked `[MapValue]`@Projection threading, rebuilt rather than restored; **D9 deleted**. (4) **D17** reclassified `StructurallyInapplicable` on A5's own measurement — `StructurallyExcusedCellCeiling` 12 → **13**, the one sanctioned raise of the round, measured in its commit. (5) The new verdict **`SurfaceEffect.EmittedInvalidCode`**, keyed on *where* the error was reported, asked before A10's rule. It **measured 10, not the expected zero, and that was surfaced rather than absorbed**: 8 are B27, 2 newly filed as **B33**. `NotCompilable` 10 → **0**, still reachable and pinned. | A6, A8, A10 follow-through; G4/R4 residue |
| A13 | `DONE` | **`[MapTo]` on a `struct` had never compiled** (`7591f25`, `ff06458`) — A11-F1. The generated registry code null-guarded a value-type source, so every one of the 14 `[MapTo]`-on-a-struct cells died on `CS0037`; all fourteen now read `Honoured` at all seven endpoints, and conformance sample F49 makes the shape runnable. Fixed through a new `TypeFacts.CanBeNull` predicate whose notion already existed **three times inline in the same file** — the ninth *guard-did-not-propagate* defect of the round, and the first where the search for siblings stopped at the first two. The two equivalent idioms on the mapper path were deliberately NOT unified: they answer differently for an unconstrained `T`. Two findings reported not fixed: **B28** and the generic-struct `CS0246` limitation. | A11-F1 |
| A14 | `DONE` | **The three `[DwarfMapperConstructor]` divergences — three endpoints, three different answers** (`4850b8b`, `d394df4`, `de94475`, `a66457c`, `1f0f3f7`). Projection **FIXED** and was worse than traced: four defects, including a `Select` whose return value was discarded and an annotated *parameterless* constructor projected as the widest one. Registry **REFUSED** as the new `DWARFR11`. UpdateInto **STRUCTURAL** — the reason is the signature shape (caller-supplied destination), not what the generator reads — with both halves pinned, including the one that cuts against it. **This is the commit range that turned the matrix GREEN.** Its own fix round caught `DWARFR11`'s message throwing `FormatException` on an unescaped brace, which no test saw because all four asserted only `d.Id`: **B22's thesis coming true on its first opportunity**, answered with a family-wide `Every_registry_message_format_actually_formats`. Filed **B29–B32**. | A11's three red assertions |

## B. Test-infrastructure holes

| # | Status | Task | Source |
|---|---|---|---|
| B1 | `DONE` | **`Scan6a` passes by construction** — searches for an enum's members in a corpus containing the enum's own declaration. `Scan2` already excludes its own defining file; copy that. Check `Scan3`, `Scan6b`, `Scan5`-options for the same shape, and the `>=40` floors sitting against actual 59 and 82. | CF §5b |
| B2 | `DONE` | **`Property`/`Field` share one `BuildAt` arm that discards `site`** — every Field cell is byte-identical to its Property cell, so a field-only divergence is invisible while reading as measured. ~11 of 162 cells. Deserves a declared **G6** entry, not a silent fix. | CF §5b.3 |
| B3 | `DONE` | **(Closed 2026-08-22, round 22 P6, `68f4cf4`.)** The `NotApplicable` excuse is now LIVE, COUNTED and ENDPOINT-EXACT. (1) Every `NotApplicable` row in `ProjectionCells` is re-classified with `OptionProbe` on every run and fails when the option becomes observable — and the check's FIRST execution caught a stale excuse: `EnumStrategy` was declared "refused before the strategy is consulted" while `MapperExtractor.Projection` has an explicit `ByValue` branch emitting a translatable enum cast; measured `Honoured`, re-declared, excuse class **8 → 7** same commit. (2) The class carries an exact shrink-only pin (7). (3) `CoversOption` takes the endpoint and matches the finding's own measured cells, so MaxDepth's span/async finding no longer excuses projection silence; the two generated-docs ratchets moved from row-level to (option, endpoint) cells. Sabotage-demoed red both ways: a doctored `NameConvention` excuse (live check + pin both fail) and NullCollections' cells moved off Projection (parity theory fails exactly there, where the old endpoint-blind lookup stayed green). | CF §5b.4 |
| B4 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** `FlattenGraph_one_source_into_two_different_collections_is_accepted` pins `("Entry","NodesA")` + `("Entry","NodesB")`: no `DWARF087`, compiles clean, and BOTH collections appear in the emitted text so an "accepted" that dropped one initializer cannot pass. Sabotage-demoed red by re-keying the refusal on the source (`seenTargets.Add(srcNavName)`), which is the mistake the pin exists to catch — the existing control next door passes under either keying, because both its source and its destination differ. Original: **No test pins that same-source, different-destination `[FlattenGraph]` stays accepted** (`("Entry","NodesA")` + `("Entry","NodesB")`). The one shape a source-keyed refusal would wrongly reject. Guards a brand-new build-breaking Error. | Task 1 review |
| B5 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** `Contracts/SurfaceFixtureBaselineTests.cs`. Measured at the reference endpoint `CreateMap`, because whether a fixture poses a question is a property of the TYPE PAIR — and there the trap is exactly as filed: the gate fires on precisely four fixtures, `case-mismatched-member` / `snake-case-member` / `internal-member` / `flattenable-nested-member`, each excepted BY NAME with the element expected to clear its `DWARF001`. The list is an obligation, not an allowlist: a named fixture must still report `DWARF001` *specifically*, so one that healed and one that rotted into another error both fail, and a second fact holds every name to a live `[SurfaceProbe]` key. **Scope ruled in-task:** run across all seven endpoints the same check also fires on five fixtures carrying `DWARF028`+`CS8795` at `Projection` and on `recursive-graph` carrying `CS7036` at `SpanMap`/`AsyncStream` (measured, recorded in the gate's doc comment) — properties of the ENDPOINT, already ratcheted by `The_cells_that_pass_both_claim_branches_are_counted`, whose own comment blesses them; gating them twice would need a per-endpoint exception store. Sabotage-demoed red in both directions. Original: Fixture-baseline rule is a comment, not a gate. **Trap:** a naive "baseline must compile" check fires on four legitimate `DWARF001`-by-design fixtures. | CF §3.2 |
| B6 | `DONE` | **(Closed 2026-08-22, round 22 P6, `0fa6737`.)** The 116-cell `NoSuchSite` population is pinned EXACTLY, PER CAUSE: `registry-has-no-mapper-class` = **48**, `no-mapping-method` = **68**, measured in the closing commit, both directions, and any cell under an unpinned cause fails outright (both pinned causes are structural, so a new cause is a slot that went missing wearing a nothing-can-be-done verdict). The cause key is the prefix of `Endpoints.SiteAbsenceReason`'s own detail string — the single source every `NoSuchSite` verdict flows from. Replaces the total-only ceiling with its ten-wide shrink band, under which the total could wander by ten AND offsetting drift summed green. Sabotage-demoed the row's exact scenario: pins swapped 47/69 (total unchanged at 116, green under the old gate) → red naming the cause. | CF §3.1 |
| B7 | `TODO` | `CrossAssembly` obligation is placement-blind — a row confined to one project satisfies the one category whose claim is that it is only observable *across* assemblies. | CF §3.4 |
| B8 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** The row asked for one guard over two stores; by the time it was implemented there was one store. `PredatesTheChangelog` was drained and DELETED on 2026-08-21 by **D-e**, so the obligation lands on `DiagnosticTestAllowlist` alone — and lands as an EXACT PIN at zero (`Scan3s_allowlist_is_exactly_empty_and_may_not_grow`), not a shrink-only ratchet: below eleven the house rule is exactness, which is why `AssertRatchet` refuses a ceiling of ten or less. Zero is the only honest value, because `Scan3` subtracts this set from its own corpus. Sabotage-demoed red with a fabricated id. Original: "Shrink-only" is prose on both `PredatesTheChangelog` and `DiagnosticTestAllowlist`. One guard covers both. | CF §3.8 |
| B9 | `DONE` | `Scan9`'s control proves the *corpus* is real, not that the *assertion* is. Sweep the scan family for the same shape. | CF §3.9 |
| B10 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** `CorpusFor_refuses_a_category_it_cannot_resolve` casts an undefined `SurfaceCategory` and asserts the throw — and the MESSAGE, because the arm's value is that it names the corpus list the next person has to extend. Sabotage-demoed red by making the arm return a corpus instead of throwing. Original: `CorpusFor`'s throwing default arm is unreached by any test. | CF §3.3 |
| B11 | `DONE` | **(Closed 2026-08-22, round 22 P6, `25d9f02`.)** `PredatesThisProject` is a counted, bounded population: exact pin **66** (shrink-only, both directions) plus an ordinal horizon bound at `DWARF077` — the highest id existing on the project's 2026-08-12 creation date, a statement of history that never moves — so a post-creation id cannot ride in even on a same-commit swap the exact pin alone would miss (the `DWARFR` family sorts after it and is correctly excluded too). Duplicates refused so the pin cannot be inflated. The two prose paths that advised "add it to PredatesThisProject" now say the truth: a new multi-assembly diagnostic is a new exemption CATEGORY needing its own counted store with its own obligation. Both guards sabotage-demoed separately: `DWARF999` added → the pin fires (67 > 66), and `DWARF002` swapped for `DWARF999` (count unchanged at 66 — the pin's blind spot) → the horizon bound fires naming the id; both reverted → 5/5. | CF §3.6 |
| B12 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** `No_option_cell_is_both_a_declared_divergence_and_structurally_inapplicable`. The two stores make opposite claims and feed two separate ratchets, so a cell in both is counted twice, neither row is load-bearing, and one fix would lower two ceilings. Still theoretical — and the reason it would STAY theoretical-looking is now written at the assertion: a structural cell renders `n/a` before it is probed, so it can never read `**SILENT**`, and the overlap would arrive as a quiet contradiction rather than as a failure. Sabotage-demoed red by adding the live `MaxDepth`@`SpanMap` cell to `StructurallyInapplicable`. Original: Nothing forbids a cell being in both `Reasons` and `StructurallyInapplicable` (double-count). Theoretical today. | CF §3.5 |
| B13 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** `AssertEvidenceLinkResolves` replaces the shape check (which `Issues/#` satisfied): the file is resolved under `RepoPaths.Root` and the anchor must be defined in it, as an explicit `<a id="…">` or as a GitHub heading slug so a future document written the ordinary way is accepted too. All 26 anchors resolve, so this closes a latent hole rather than finding a break. Sabotage-demoed red by pointing `MaxDepth`'s `Section` at an undefined anchor. Original: `SurfaceParityTests` checks the evidence link's *shape*, not that file and anchor resolve. Latent. | CF §5b.5 |
| B14 | `DONE` | **(Closed 2026-08-22, round 22 W5.)** The remarks now state the collision — another generator in the consumer's compilation, or a checked-in `.g.cs`, is called generator-authored and its hand-written manifest attribute escapes `DWARF086` — and why it is accepted: the failure is PERMISSIVE-ONLY, it can only withhold a diagnostic and never redden a build that should be green, and the alternative discriminators are worse (the path-emptiness one is written up directly above). Documentation only, so the one item in the W5 batch with no sabotage demo: there is no assertion to redden. Original: `IsGeneratorAuthored`'s remarks omit the `*.g.cs` collision case (permissive-only, undocumented). | CF §3.7 |
| B15 | `DONE` | **(Closed 2026-08-22, round 22 W1, `390936d`.)** The discard is now the new **`DWARFR12`** (Warning): the behaviour is kept — the annotated source member is still ignored — but the argument the caller wrote is reported instead of dropped, ending the asymmetry with `DWARF089` at the co-located host. Decided as one policy with **B20**/**B21** (the same silence seen from the registry side). Full five-file sync minus a `NegativeCases` file, which the whole `DWARFR` family has never had — the trigger and its bare-form neighbour are pinned in `RegistryDiagnosticsGenTests` instead. Sabotage: the guard inverted fails the whole-solution build on the Gallery's own bare `[MapIgnore]`. Measured: build 0W/0E, suite **7,798/0**, matrix **866/866** with no population or ceiling moved — the Registry `MapIgnore ctor(1)` cells this row named stay Refused, `DWARFR12` being an additional warning rather than a changed verdict. Original record: **The registry accepts `[MapIgnore("x")]` on a member and discards the argument** — a silent discard of exactly the shape this round hunts, and now an asymmetry: `DWARF089` refuses the identical text at the co-located host. Decide one way for both. No cell measures it (the matrix's Registry `MapIgnore ctor(1)` cells are refused by `DWARFR02` for an unrelated reason), which is why it survived. **This is the PRODUCT item and it is still open.** The task executed under the name "B15" was a different thing entirely — see **B34**; the id collided and nothing here was touched by it. | A4 |
| B16 | `TODO` | **`DWARF088`'s message calls a co-located `[GenerateMap]` host "this mapper class".** It is one, in the sense `ExtractCore` means; it is not one to the reader who wrote a plain DTO. Pre-existing from A2, not made worse by A4. | A4, A2 |
| B17 | `TODO` | **The `StringFormat` path leaves the converter it replaced in the synthesized-helper table**, so an unused `private static` helper is emitted next to the formatted one. Cosmetic, affects every endpoint that honours `StringFormat`, harmless but it is generated code nobody asked for. | A4 |
| B18 | `TODO` | **A member-form `[MapProperty]`/`[MapIgnore]` on a MEMBER of a `[DwarfMapper]` class is swallowed** — `DWARF088` is raised off the class and method symbols, never off a member, so the one placement left is silent. Predates A4 (mode-1 `[GenerateMap]` included) and **no cell measures it**: the matrix's member sites at the five mapper endpoints splice into the DTO pair, never into the mapper class. `CoLocatedHostMemberDirectiveTests.A_DwarfMapper_class_declaring_a_pair_does_not_read_its_own_members` currently sits over this shape and says in its remarks that it does not bless it. | A4 review |
| B19 | `DONE` | **(Closed 2026-08-22, round 22 W6 — the DOCUMENTATION half, which is all this row asked for.)** The limitation is now written where the ceilings are read: a block in the `SurfaceParityTests` class doc — the file every one of those ceilings is declared in — states that `Honoured` proves DIFFERENCE and not CORRECTNESS, carries the A7 infinite-recursion evidence and D2's wrong-diagnostic case for the weaker `Refused` form, says explicitly that no remedy is scoped there, and points at **R22-01** (the differential oracle that RUNS generated maps and compares against a reference interpretation) and **R22-02** (metamorphic relations over the result rather than the diff) as the systemic pressure — both of which landed this round at `b86cd0d` and `dbc96db`. A companion note sits on `SurfaceEffect.Honoured` itself, where the verdict is produced. The closing sentence is the point of the row: read these ceilings as *no element silently stopped acting*, never as *the generator is right*. Original: **`Honoured` proves an element had an effect, not that the effect is right — so a cell can be GREEN while the generated code is broken.** `SurfaceProbe.Classify` returns `Honoured` when the emitted text merely *differs* between the with-and-without compilations. Nothing inspects what it differs *into*. **Evidence, from A7:** `[AfterMap]` on `public partial void Update(Src s, Dst d)` made the generator emit `Update(s, d);` as the last statement *of `Update`* — unconditional infinite recursion, shipped, compiling. The matrix scored that cell **`Honoured`** and the claim-parity theory passed it, at both the claimed and (had it been unclaimed) the honest reading. It was found only because D16's *neighbouring* cell at SpanMap was `Silent` and someone dumped the generated body while chasing that one. This is a different class of failure from everything else in this file: every earlier finding was the matrix failing to **measure** something, and was therefore visible as a red or unaccounted cell. This one **graded broken behaviour as working**, and left no trace anywhere in the counts. A `Refused` cell has the same blind spot in weaker form — it proves a diagnostic was added, not that it was the right diagnostic (see D2, where a `DWARF038` about an `int → string` conversion was filed for four rounds as a refusal of `[MapProperty]`'s placement). **Do not scope the remedy here** — "how does a cross-product of 854 cells assert *correctness* and not just *difference*" is a design question, not a fix, and the cheap answers (golden output per cell, a runtime execution leg, a recursion/self-call check on generated bodies) differ enormously in cost and in what they actually catch. What this item asks for is that the limitation is written down where the ceilings are read, so the next person does not mistake a green matrix for a correct generator. | A7 |
| B20 | `DONE` | **(Closed 2026-08-22, round 22 W1, `390936d`.)** It is now the new **`DWARF095`** (Warning), the `DWARF056` shape the row predicted. A METHOD-site name is judged against that method's own destination; a CLASS-site name is class-wide and judged against every pair the class maps, so a class ignore about one of two pairs stays legitimately silent on the other. A name matching only a span/async ELEMENT pair remains `DWARF090`'s report — one attribute must not earn two ids pointing in two directions. Where the class carries an endpoint whose unscoped-ignore consumption the walk cannot see (`[MapDerivedType]` dispatch, a top-level collection map) the class-site verdict **stands down entirely** rather than guess: a false "names nothing" would break a working suppression, which is **B19**'s exact genre. Both stand-down set-sites are pinned (the dispatch sibling was unpinned — round 20's "the guard did not propagate" rule). The liveness set is resolution's own — writable members PLUS read-only ones, because an ignore on a read-only member legitimately suppresses the silent-loss warning `DWARF007` itself prescribes it for. Nested pairs take pair-scoped `[MapIgnore<T>]` only, verified not assumed, and pinned so that wiring cannot change in silence. The reliance check the row demanded was answered from both sides: whole-solution build (samples included, warnings-as-errors) is 0 warnings, and inverting the registry sibling's guard proves the Gallery's bare `[MapIgnore]` is live. Measured: build 0W/0E (33.0 s), suite **7,798/0** (was 7,779), matrix **866/866**. Original record: **A `[MapIgnore]` that names nothing is silently inert at every endpoint.** The destination-name set is matched with `MapperExtractor.IgnoreNameComparer` (ordinal), so `[MapIgnore("id")]` against a property `Id` — or `[MapIgnore("Typo")]` against nothing at all — is accepted, excludes nothing, and produces **no diagnostic anywhere**. The caller believes they excluded a member; the completeness gate goes on demanding it, and `DWARF001` (if it fires at all) names the member rather than the dead directive. Same silent-discard shape as **B15**, and it is what made A7's comparer question ambiguous enough to get wrong: with no diagnostic to anchor on, "does this name work?" had to be answered by reading the set's construction. A `DWARF056`-style "matched nothing" report is the obvious candidate — that diagnostic already exists for the *pair-scoped* forms (`[MapIgnore<T>]` naming no pair), so the unscoped form being exempt is an asymmetry, not a policy. **Check first whether any test or sample relies on a dead `[MapIgnore]`** before making it loud. | A7 review |
| B21 | `DONE` | **(Closed 2026-08-22, round 22 W1, `390936d`.)** Ruled and pinned: **directive names bind ORDINALLY under every option.** `CaseInsensitive` fuzzes AUTO-MATCHING between source and destination names, never the binding of a name the caller wrote — the same rule `[MapProperty]`'s source and target names already follow at resolution. So under `CaseInsensitive = true`, `[MapIgnore("id")]` does **not** exclude `Id`; what changes is that it is no longer silent about it (**B20**'s `DWARF095`), which is what made this question answerable rather than a matter of reading the set's construction. Pinned in BOTH directions in `UnscopedIgnoreNoMatchTests` and again in the `DWARF095` `NegativeCases` file; sabotage-demoed by flipping the one comparer home to `OrdinalIgnoreCase` (red, reverted). Original record: **`[DwarfMapper(CaseInsensitive = true)]` versus the always-ordinal ignore set — measure, do not assume.** Member *matching* can be made case-insensitive by that option, but `IgnoreNameComparer` is ordinal unconditionally. So under `CaseInsensitive = true` a source `id` maps to a destination `Id`, yet `[MapIgnore("id")]` plausibly *should* exclude `Id` and does not. **Not asserted as broken** — a defensible reading is that a directive names a member exactly while auto-matching is fuzzy, and CLAUDE.md records that the repo's one `CaseInsensitive` doc fence needs a deliberately lower-cased fixture, so the shape is under-exercised generally. What is missing is that **nothing establishes either answer**: no test pins ignore behaviour under that option, and the surface matrix cannot see it (the option is a `[DwarfMapper]` axis, the ignore is a separate element, and no cell varies both). Decide it, then pin it — the comparer now has one home (`MapperExtractor.IgnoreNameComparer`) so the change would be one line plus tests. | A7 review |
| B22 | `TODO` | **No `DWARFR##` message or remedy is pinned anywhere — the whole registry family, not one id.** Every `DWARF0xx` id has its wording held in place: `DiagnosticMessageContractTests` and `DiagnosticProseIsCurrentTests` pin message shape and prose, `Scan9` forces a `CHANGELOG.md` entry per id, and `DiagnosticCoverageRatchetTests` wants a `NegativeCases` row. The `DWARFR` family is **excluded from all of them by written convention** — `AssemblyScanTests.cs:74-80` states the `Scan9` exclusion explicitly (the family is announced as a *range*), and `docs/diagnostics.md` declares `[MapTo]` a prototype tier exempt from the DWARF0xx self-validation scans. So the family's only gate is `RegistryDiagnosticsGenTests`, which checks that each id is **triggered** by some test and **mentioned** in `docs/diagnostics.md`. Neither is a check on WORDING: a `DWARFR` message can be rewritten, have its remedy inverted, or stop naming a fix at all, and nothing fails. **Surfaced by A5**, which added `DWARFR10` and followed the convention correctly — the id's message names two remedies and both are unpinned. Two concrete instances to settle while deciding the mechanism: (a) `DWARFR10` offers `[MapIgnore]`, which at the registry ignores the *source* member and so leads straight to `DWARFR02` — the registry has no destination-side skip, unlike `[MapIgnore("Dest")]` in the class model, so one of the two offered fixes takes two steps (disclosed and accepted at A5 review, since the `[MapProperty]` remedy does terminate in one); (b) `DWARFR02`'s own text says "add a source member, a `[MapProperty]` binding, or remove it", which is the fix a `DWARFR10` reader has just been sent away from. **The decision is whether to fold `DWARFR##` into the DWARF0xx scheme** (`RegistryDiagnostics.cs` has said "a later unification could fold them in" since it was written) **or to give the family its own wording gate.** Folding is the larger change and settles it once. | A5 review |
| B23 | `DONE` | **(Closed 2026-08-22, round 22 W4, `072c7ca`.)** `LoadOptions.PreserveWhitespace`, applied and regenerated. The full diff was reviewed hunk by hunk and moves in exactly four places, every one a single space INSERTED: `DWARF038build error`, `System.Text.JsonIgnoreCycles`, `constructedfrom the class`, `System.Text.JsonReferenceHandler.IgnoreCycles`. Four adjacent-inline-tag sites exist in `src/DwarfMapper`'s public doc comments and there are four hunks, so the diff is complete; no word is removed or reordered; the other two generated documents do not go through `XDocument` and did not move. Sibling hunt: exactly one `XDocument.Load` call site in the whole of `src`+`tests`+`scripts`. A NON-SELF-REFERENTIAL pin came with it — `ApiReferenceRendererTests` compares a hand-written expectation against the real file-load path through a new `internal ParseSummaries(string)` seam, because an in-memory `XElement` fed to `Flatten` would have passed before the fix too; sabotage-demoed red by removing the load option. Original: **`ApiReferenceRenderer` silently eats a space between two adjacent inline doc tags.** `ApiReferenceRenderer.cs:189` calls `XDocument.Load(xmlPath)` with default `LoadOptions.None`, which **discards whitespace-only text nodes**. So wherever a doc comment separates two inline elements by nothing but a space — `</b> <c>`, `</c> <see …/>`, `</see> <c>` — the space is gone before `Flatten` ever runs, and `docs/generated/api-reference.md` renders `classes.AutoMatchMembers` for source that plainly reads `classes.</b> <c>AutoMatchMembers</c>`. `Flatten`'s own `Regex.Replace(@"\s+", " ")` cannot restore what the loader dropped. **Found by A5** — twice: once in the generated output, then again because A5's report claimed a reword to `<c>` had fixed it when re-reading the regenerated file showed it had not. Only the *authored* comment was reworded (real text now sits between the tags), so the renderer defect is untouched and latent for every future doc comment. Fix is one argument — `LoadOptions.PreserveWhitespace` — but it is shared doc tooling whose output is byte-compared by `GeneratedDocsAreCurrentTests`, so it must be applied with the whole regenerated diff reviewed, which is why A5 filed it rather than folding it in. **No test would have caught this**: the docs test compares the generated file against the renderer's own current output, so a renderer bug is self-consistent and invisible to it. | A5 review |
| B24 | `TODO` | **The same pair named twice with CONTRADICTING `[MapNullSkip<S,T>]` values silently discards the second.** `MapNullSkipAttribute<TSource, TTarget>` is `AllowMultiple = true`, so `[MapNullSkip<Dto, Entity>(true)]` beside `[MapNullSkip<Dto, Entity>(false)]` on one class compiles clean. `MapperExtractor.ResolveNullSkip` returns the **first** match by declaration order and the second directive is dropped without a word — a caller's explicit, opposite statement of intent, discarded, which is the exact shape this round hunts. Distinct from the method-vs-pair contradiction A6 DID define (most-specific-wins, pinned both directions): there the two forms have a defensible ranking, here they have identical scope and only source order separates them, so there is nothing to rank. Deferred rather than fixed because refusing it is a new diagnostic and needs the full five-file sync; `DWARF056`'s "matched nothing" pass is the closest existing shape. Found while unifying the readers, not measured by a test — the surface matrix cannot reach it, because its `×2` axis renders two IDENTICAL applications on purpose (a `bool` samples to `true` for both variants), so no cell in the 865 poses this question. | A6 |
| B25 | `DONE` | **(Closed 2026-08-22, round 22 W2, `b53e6f8`.)** `MapValueAttribute<TTarget>` now carries the same two `[DwarfSurfaceProbe]` declarations its arity-0 twin was corrected with — `Arguments = "{Name}"` and `"{Name}, \"probe\""` — so `Name` (the flat pair's string member) receives the string constant instead of `Id` (an int). Re-measured, before → after on all 28 cells: `DWARF040,DWARF064 (behind CS8795)` → **`DWARF064` alone** at CreateMap/SpanMap/AsyncStream/CoLocatedHost — the type error is gone and what remains is the shadow-Info the non-generic twin also draws on this fixture. **No counted population moved**: `UnaskableCellCeiling` 44, `StructurallyExcusedCellCeiling` 13, `UnhonouredButLoudCellCeiling` 14 all re-measured identical; `NotCompilableCellCeiling` and `EmittedInvalidCodeCellCeiling` exact-pinned 0 and green; matrix 866/866. The ×2 multiplicity axis now renders two identical applications (a declared `Arguments` list does not vary by variant) — a property inherited from the arity-0 precedent, not introduced here, and the cell it produces is the honest `DWARF042` duplicate-target refusal. The row's second half — the unexamined `DWARF056` readings at UpdateInto/Projection — **was examined and is real**: pair-scoped `[MapValue]` is consulted at three sites and neither of those two is one of them, so the pair is mapped and the directive is not read, while `DWARF056` says "matches no mapped pair". The remedy is a design choice, filed as **I9**. Original record: **`[MapValue<T>]`'s probe arguments are still the nonsense shape the arity-0 form was corrected out of.** `MapValueAttribute<TTarget>` declares `[DwarfSurfaceProbe(… Arguments = "{Id}, \"probe\"")]` — a string constant assigned to an `int` — so all 28 of its cells measure the generator's reaction to a **type error** (`DWARF040`) rather than to a constant assignment. Measured while working D9: `[MapValue<Dst>("Id", "probe")]` reads `NotCompilable (CS8795)` at CreateMap, SpanMap and AsyncStream, and `Refused (DWARF056)` at UpdateInto and Projection; with `{Name}` and a string it reads `Honoured` at SpanMap and AsyncStream (that measurement is what pinned `DWARF090`'s remedy). Its arity-0 twin was fixed exactly this way in an earlier round and the generic form was left behind. **Out of A8's cell list** — D9 is arity 0 only — but it is the same stale-evidence pattern this round exists to find, and the DWARF056 readings at UpdateInto/Projection are an unexamined claim of their own: the pair-scoped form apparently does not reach those two endpoints. | A8 |
| B26 | `DONE` | **(Closed 2026-08-22, round 22 W2, `b53e6f8`.)** `ResolveFlattenInfos` no longer reports: it returns the nullable-hop verdict per root (`NullableHop`), and the resolver reports `DWARF044` at the end of its walk, off the set of roots a destination member actually pulled a leaf up from — the same shape and the same reason as the `DWARF070` report two lines above it (the answer is only knowable after every pass has had its chance to consume or refuse a leaf). Reported once per root and ordered by root name, so output stays deterministic. Projection is untouched by construction: it passes `warnNullableHop: false`, so its verdict is always false. Pinned in both directions — a nullable root whose leaves land nowhere reports nothing (and the generated code contains no `Address.` access to be unguarded), and with two nullable roots where only one lands a leaf, exactly that one is reported. Sabotage-demoed: drop the consumption test from the filter → both new tests red, reverted → 6/6 green. **No matrix cell moved**: the `[Flatten]` probe fixture's leaves do land, so its CreateMap/UpdateInto/Projection cells already read `Honoured — output differs` and still do; what this removes is the mechanism that made **D10**'s evidence false, not a current false reading. Whole-solution build 0W/0E; suite 7,800/0; matrix 866/866. Original record: **`DWARF044` fires for a `[Flatten]` root whose leaves land nowhere.** `ResolveFlattenInfos` warns about a nullable-reference root as soon as the root resolves, before anything asks whether a single leaf matches a destination member — so a flatten that maps nothing at all still warns that "a null value throws at runtime when its flattened members are read", about members nobody reads. Harmless in isolation; not harmless as evidence, because it is precisely what made **D10** read `Refused` at CreateMap and UpdateInto for four rounds while the emitted output was byte-identical, and therefore what let the finding claim the directive was honoured there. A diagnostic that fires on a directive with no effect is a cell that looks measured and is not. Cheap fix (warn only when `flatMatches` actually consumed a leaf), but it changes an existing warning's trigger, so it is filed rather than folded into A8. | A8 |
| B27 | `DONE` | **(Closed 2026-08-21, round 21 T6 — the gap is now `DWARF094`, an Error, five-file sync complete.)** The detection lives in the same signature pass as `DWARF060`, where identical-signature-with-IDENTICAL-return used to fall through a `continue` labelled "a duplicate-pair concern" that nothing downstream owned. Two shapes, one gate, remedy sentence per shape: the same pair declared twice (also the ×2 matrix axis — and the co-located host cell, whose template already declares the pair), and the pair declared beside a same-named `partial B Map(A)`. Emission of the synthesized duplicate is dropped (`DWARF060`'s pattern) and the Error suppresses the class, so the consumer sees the refusal — never `CS0111`/`CS0121` out of a `.g.cs` — pinned both ways (`DuplicateGenerateMapSignatureTests`, NegativeCases `DWARF094_DuplicateGenerateMapSignature`). `DWARF093`'s sharp-edge sentence now names `DWARF094` instead of narrating the raw `CS0111` (message, docs and its wording pins updated together). Sibling search: wrapper-expanded pairs flow through the same `genPairs` list AND `ExpandWrapperMaps` already defers to an explicitly-declared closed pair — that guard is now pinned so removing it fails a test rather than resurfacing as broken emission; legal neighbours (differently-named partial, shared-source `DWARF060` territory, shared-target overloads, multi-pair classes) pinned per the B4 lesson. All 8 cells read `Refused`; `EmittedInvalidCodeCellCeiling` 8 → **0** re-measured — the population the repository says must not exist, actually emptied. Original record: `[GenerateMap<A, B>]` beside a `partial B Map(A)` over the same pair was a bare `CS0111`, with no DWARF diagnostic. `[GenerateMap]` emits its own `public B Map(A src)`, and a class that also declares `public partial B Map(A s)` gets *"Type 'M' already defines a member called 'Map' with the same parameter types"* from the compiler plus a cascade of `CS0121` ambiguities — the generator says nothing about a collision it created. `DWARF060` covers the adjacent shape (two `[GenerateMap]` from one source type, and its own history is that it *replaced* a `CS0111`), and `DWARF057` covers a generated mapper NAME colliding with an existing type, so a generated *member* colliding with a declared partial is the gap between them. **Found by A9b**, because it is the sharp edge of `DWARF093`'s remedy: the obvious way to give `[GenerateWrapperMap]` a pair to expand is to add `[GenerateMap<A, B>]`, and on a create-map class that lands the caller in `CS0111`. `DWARF093`'s message names the case explicitly and a test measures it, so nobody is sent in a circle — but the underlying collision deserves to be the generator's own diagnostic rather than a sentence inside somebody else's. | A9b |
| B28 | `TODO` | **The registry pre-sizes a collection buffer from `s.Count` for any type that merely *implements* `ICollection<T>`/`IReadOnlyCollection<T>`, without checking `Count` is a public instance member.** `CollectionConverter.TryGetEnumerableElement` (`CollectionConverter.cs:960`) walks `Self(src)` and sets `CountKind.Count` the moment either interface appears anywhere in `AllInterfaces`; `MapToGenerator.CountExpr` then emits `s.Count` as the `List<T>` capacity argument. Implementing the interface is not the same as exposing the member: **`ImmutableArray<T>` implements both EXPLICITLY**, so `s.Count` does not bind as an instance member and the emitted helper does not compile. Two failure modes, and the second is the dangerous one. In the generated file as the generator writes it — no `using` directives at all — it is **`CS1061`** (*'ImmutableArray<Leaf>' does not contain a definition for 'Count'*), a clean build break. In a **consumer project with implicit usings on**, `System.Linq` is in scope, so `s.Count` binds to the LINQ extension **method group**, the capacity overload no longer matches, and the compiler reaches for `List<T>(IEnumerable<T>)` instead — **`CS1503`**, *cannot convert from method group to `IEnumerable<T>`*. It is a build break either way today, but the mechanism is overload selection quietly moving to a different constructor, which is a worse failure than the one it currently produces. Nothing to do with nullability, and **not confined to value types**: any type — reference types included — whose `Count` is an explicit interface implementation trips it. **Found by A13** while probing the fourth null-guard site with an `ImmutableArray<T>` member; the fixtures there use a user-declared `readonly struct … : IEnumerable<T>` (no `ICollection<T>`, hence `CountKind.None`) precisely so that a fixture failing for two reasons could not be mistaken for the one A13 closed. **Deliberately not fixed there**: it is pre-existing, outside A13's scope, and a `CountKind` change touches collection emission broadly — the class engine reads the same helper. The fix is presumably a member lookup (is there a public instance `Count`/`Length`?) rather than an interface test, plus a decision about whether `Length` should be preferred where both exist. **This is the shape the round has been closing all along — a predicate that is correct only for the inputs which reach it today** — and `TypeFacts.cs`'s own doc comment says so in as many words; the fourth null-guard site proved that warning right inside the very commit that wrote it, so whoever picks this up should read it as another instance and not an isolated curiosity. | A13 |
| B29 | `TODO` | **The projection's remaining constructor pick is widest-arity-wins with no usability filter and a silent tie.** A14 routed the projection endpoint through `ConstructorSelector`, but ONE branch still falls back to the file's original expression: when the selector answers "parameterless" and member-init cannot carry the target (no writable members), `ChooseProjectionConstructor` picks `InstanceConstructors.Where(public && !static && Parameters.Length > 0).OrderByDescending(Parameters.Length).FirstOrDefault()`. That is missing every filter `ConstructorSelector.IsUsableCandidate` applies — most sharply the **record copy constructor** (`R(R other)`, which for a single-parameter positional record `R(int X)` ties on arity and can win on declaration order), plus `[Obsolete]` and `ref`/`out` parameters (CS1620) — and where the selector reports an arity tie as `DWARF025`, this resolves it silently by source order. **Deliberately retained at A14** so that a target the selector answers "parameterless" for keeps projecting the way it always has; closing it changes WHICH constructor existing projections call, which is a behaviour change needing its own before/after measurement and its own commit. The honest fix is probably not a second filter here but teaching `ConstructorSelector` that object-initializer construction is unavailable for this target — the same idea its `skipImplicitParameterlessCtor` rule already encodes for structs — so the endpoint stops needing a fallback at all. | A14 |
| B30 | `DONE` | **(Closed 2026-08-22, round 22 W3, `60d8d8c`.)** The `HasParameterlessCtor` test is now asked at BOTH types the front door constructs — the `[MapTo]` target and every type `SynthNested` builds, which is also the path a collection ELEMENT reaches. **Ruling on the open question: ONE id, not a new one.** A second id would restate the same refusal in different words and re-open the very asymmetry this closes; the hesitation — the remedy sentence reads differently when the type at fault is not the annotated one — is a WORDING problem, and `DWARFR11` (given both sites when it was minted) already shows the fix. So `DWARFR09`'s title now names the registry rather than the target (*"A type the `[MapTo]` registry constructs has no accessible parameterless constructor"*) and its `MessageFormat` is `"{0}"`, composed per site: the nested one names the type and the member it was reached through, with the member's nullable annotation dropped. **Sibling sweep of the shape**: the registry constructs exactly two kinds of type — `TryCollection` builds only `List<T>` and arrays, neither of which can lack a parameterless constructor, and its element goes through `Resolve`, so there is no third site and no dictionary path here. **Found and fixed alongside** (the new refusal inherited it): a loud nested refusal also drew `DWARFR05` *"member types are incompatible"*, which is false — `Resolver.RefusalReported` now suppresses the companion, which also ends the PRE-EXISTING cascade where a recursive nesting drew one false `DWARFR05` per nesting level. Pinned both ways (nested + element refused with nothing emitted; a constructible nested type still emitted) and a genuine no-conversion on another member still reports. Sabotage: guard disabled → 3 red including the assertion that `new global::Demo.LeafDto` reaches the compiler; reverted → 43/43. Measured: build `--no-incremental` 0W/0E with samples, suite **7,806/0**. Original record: **`SynthNested` has no `DWARFR09` equivalent: a constructor-only NESTED registry target still reaches the compiler as `CS1729`.** `MapToGenerator.Extract` refuses a `[MapTo]` target with no accessible parameterless constructor as `DWARFR09`, and that descriptor's own comment states why — *"the registry constructs targets with `new T { … }`… A ctor-only target also has no writable members, so the completeness gate stays silent and the failure surfaced only as CS1729 out of generated code."* Every word of that is still true one level down: `MapToGenerator.Resolver.SynthNested` writes `new {fqTgt}` plus an initializer with **no parameterless-constructor check at all**, and it is also the path a collection's ELEMENT type reaches (`TryCollection` → `Resolve` → `SynthNested`). So a nested member — or a `List<T>` element — whose type is a positional record or otherwise ctor-only produces exactly the error `DWARFR09` was written to replace, with no diagnostic. **Found by A14** while placing `DWARFR11`'s second call site, which is the same pair of construction sites: the new refusal was given both, this older one has only the first. Textbook sibling-guard-not-inherited, the shape this round has closed nine times. Fix is presumably to hoist the `HasParameterlessCtor` test the way `ReportUnreadConstructorDirective` was hoisted and call it from both; the open question is whether a nested failure should report `DWARFR09` naming the nested type or earn its own id, since the remedy sentence ("add one, or use the `[DwarfMapper]` class model") reads differently when the type at fault is not the one the caller annotated. | A14 |
| B31 | `TODO` | **`[DwarfMapperConstructor]` on a constructor the selector cannot use is silently ignored at every endpoint that reads it.** `ConstructorSelector.IsUsableCandidate` filters an annotated constructor that is non-public (without `AllowNonPublic`), `[Obsolete]`, a record copy constructor, or has `ref`/`out` parameters — and selection then falls back to the parameterless object-initializer path **without a word**. That fallback is deliberate and documented in the selector ("an unusable annotated ctor falls back to the (safe) parameterless object-initializer path rather than producing broken output"), and it is the right EMISSION; what is missing is the report. The caller wrote a directive naming one specific constructor, the generator declined to use it for a reason it knows exactly, and the build says nothing — a caller who marks a `private` constructor and gets object-initializer mapping has no way to learn why. **Measured at A14** at CreateMap and Projection: annotated private constructor, no diagnostic at either, output identical to the unannotated baseline. Not the same shape as the round's other silences — this one is silence about a MALFORMED declaration rather than about a well-formed one, so the surface matrix cannot see it (its cases render legal placements), which is precisely why it needs filing rather than measuring. A new id, so it needs the full five-file sync; `DWARF056`'s "matched nothing" family is the closest existing shape. Deciding it should also settle whether the ANNOTATION being unusable and the annotation being absent deserve different messages, since only the first is a caller mistake. | A14 |
| B32 | `TODO` | **A `[DwarfSurfaceSite]` narrowing is validated for FORM and counted by NOTHING — the set of dropped endpoints can grow unratcheted forever.** `SurfaceCatalog.ValidateSiteClaims` checks that a site claim names a legal site, does not overlap another, states a reason, and does not restate the element's default — all shape, none of it a limit. The matrix's one structural-excuse ratchet, `StructurallyExcusedCellCeiling`, cannot see these: `SurfaceParityTests.StructurallyInapplicableOption` parses an axis of the form `Name=value` and looks the name up in `DeclaredDivergences.StructurallyInapplicable`, so it covers **one option of an option bag** and returns `null` for any directive axis (`ctor(0)` has no `=`). The asymmetry is stark against `DeclaredDivergences`, where a row is BOTH counted against a shrink-only ceiling AND re-measured every run so it fails the moment its cell starts working. A site narrowing gets neither: nothing counts how many endpoint-claims have been dropped across the surface, and nothing fails when a dropped endpoint later starts acting — which is the same non-self-retiring hazard `The_cells_excused_as_structural_are_counted` was written for, one level up. **Surfaced by A14**, whose UpdateInto answer is the first directive-level structural narrowing on this branch; it is mitigated there only by per-instance diligence (a test pins the narrowing's own premise — that the endpoint constructs nothing — and fails if that stops being true), and per-instance diligence is exactly what a ratchet exists to replace. Two things to decide: a counted population for dropped (element, site, endpoint) triples, and whether the premise-pin should be a *convention* the catalogue can enforce rather than a habit. Note the brief for A14 predicted this narrowing would raise `StructurallyExcusedCellCeiling` and it structurally could not — the accounting gap is already misleading readers. | A14 |
| B33 | `DONE` | **(Closed 2026-08-21, round 21 T6 — the emission now threads the tracker.)** `EmitSpanMapMethod`/`EmitAsyncStreamMapMethod` now allocate ONE shared `DwarfRefContext` per call and pass the `(ctx, 0)` tail to a ctx-carrying element converter — the same identity-map scope the top-level collection path already gives a `List<T>` map, which is what made correct-emission the non-ambiguous choice over a refusal. Two span slots / stream elements holding the same source object land the SAME target instance under `Preserve`; a diamond spanning two elements stays one child. The sibling hunt paid twice: the identical missing tail was reachable with no `Preserve` in sight (a recursive element pair under default None → same `CS7036`; ditto `OnCycle = SetNull`, where additionally the SetNull post-pass skipped span/async models because it read their PARAMETER type — a span struct — where the ELEMENT is what cycles, so the shared ctx would have been allocated without its stack set). All three modes sabotage-verified red pre-fix and pinned by EXECUTING tests (`ElementWiseReferenceHandlingRuntimeTests`), per B19's warning that the matrix grades difference, not correctness. `EmittedInvalidCodeCellCeiling` 10 → **8** re-measured; matrix 866/866. Original record: `[DwarfMapper(ReferenceHandling = Preserve)]` emitted a span map and an async-stream map that did not compile — `CS7036` in the generated file. `Preserve` adds a reference-tracker parameter to the mappings it emits, and the element-wise emission calls the element mapper without passing it, so `Demo.M.g.cs` carries *"there is no argument given that corresponds to the required parameter"*. The consumer gets a broken build in a file they never wrote, at two endpoints, with no DwarfMapper diagnostic — which is the shape `DWARF028` covers for projection (*reference handling is not supported*, an honest refusal) and nothing covers here. Either the element-wise emission threads the tracker, or the two endpoints refuse `Preserve` the way projection does. **Surfaced by A12**, when the new `EmittedInvalidCode` verdict re-read these two cells: they had been sitting in `NotCompilable`'s population, whose own doc comment described them as *"the hand-written partial declaration in the endpoint template fits no generated overload"* — that would have been `CS8795` in the caller's file, and the error is in the generated one. The label said there was nothing to judge; the measurement says the generator is emitting invalid C#. Counted by `EmittedInvalidCodeCellCeiling` (10, of which these are 2; the other 8 are **B27**). | A12 |
| B34 | `DONE` | **The batched doc-accuracy cleanup executed under the name "B15"** (`15a93ab`, `1be82b3`, `eddb0c0`, `1cd99a9`, `d26fda8`). Twenty items: 16 fixed, 2 already fixed, 1 partial by design (`DWARFR04`'s message now composes a remedy per arity mistake; the `helpLinkUri` was withheld because that gap is class-wide across the `DWARFR` family, not `R04`-specific), and **1 skipped for a good reason** — see **B35**. Item (o) landed as a real guard: the `DWARF078` exclusion now lives *inside* `IsGeneratorRefusal` with a pinning test, so a refactor cannot silently reorder it. Its twenty-item worklist lives only in the git-ignored `B15-items.md`, which is why this row states the outcome rather than pointing at it. | final review |
| B35 | `TODO` | **`DWARFR04` co-fires with `DWARFR01` on an invalid `[MapTo]` target, and the filed remedy was wrong.** B34's item (a-A2) proposed restoring the old `targetCount > 0` guard. It cannot work: `targetCount` is computed from the raw `[MapTo]` argument list **before** `IsMappableTarget` filters invalid entries, so an invalid-but-present target already makes it `> 0`. The guard would only change an unrelated zero-target edge case. Left untouched and flagged rather than "fixed" — the tenth filed remedy this round that did not survive contact with the code. **A different remedy is needed and none is proposed yet.** | B34 item (a-A2) |
| B36 | `TODO` | **`DWARF090` and `DWARF092` both mean "the directive is not read here", and they split by DIRECTIVE rather than by OUTCOME.** So `[Flatten]` + `[FlattenGraph]` on one span-map method yield two different ids for one situation. Defensible as filed — the two carry different remedies, which is the whole point of separate ids — but a reader who meets both at once has no way to know that from the messages. Decide whether the split is worth stating in the prose of both. | final review, src leg |
| B37 | `TODO` | **A `[DwarfSurfaceProbe]` with a declared `Arguments` list collapses the ×2 multiplicity axis to two identical applications.** `SurfaceCatalog.ArgumentsFor` returns `ExpandArguments(declared)` without consulting `variant`, so the second application of a declared-arguments element is byte-identical to the first — exactly the degeneracy the `SampleArgument` doc comment says the variant dimension exists to prevent (*“Variant must vary so the multiplicity axis renders two DIFFERENT applications”*), fixed there and left unfixed one method up. Affects every element with a declared list: both `[MapValue]` forms (where the two identical applications now read as the honest `DWARF042` duplicate-target refusal rather than asking a second question), `[Flatten]`, `[FlattenGraph]`, `[MapCollectionKey]`, `[MapIgnoreSource]`, `[Reinterpret]`, `[MapDerivedType]`, `[AutoNest]`. Found at W2 while re-measuring B25: the generic `[MapValue<T>]` rendered `("Id", …) + ("Name", …)` before the probe fix and two identical `("Name", …)` after, so the fix that made the cells measure the directive also flattened that one axis — a property inherited from the arity-0 precedent, not introduced by it. **Not maintainer-blocked**: `ArgumentsFor` could rotate the `{Member}` placeholders by variant the way `SampleArgument` rotates its names, which would restore the axis for every declared-arguments element at once. Cheap, but it moves renderings across the matrix, so it wants its own re-measurement rather than a ride on someone else's commit. | W2 (B25) |

## C. Tooling and environment

| # | Status | Task | Source |
|---|---|---|---|
| C1 | `DONE` | **Stryker runs in no CI job at all.** Closed by the mutation stream merged at **`d2d54cf`**: the leg now exists in `ci.yml` as a nightly cron plus `workflow_dispatch`, dormant until the branch lands on master. The committed runtime config carries no exclusion — no `test-case-filter`, no `test-projects` — per the maintainer ruling, `break: 61` at merge (**87** since the round-21 T7 re-measure), with a comment stating the measurement, the date, and that `66` must not be restored. | CF §5b.1 |
| C2 | `DONE` | **`.git`-as-a-file breaks the doc tests in any worktree** — and forced Task 1 to hand-render a generated file. Fix `RepoRoot` to accept both. | CF §4.1 |
| C3 | `DONE` | Both sibling Stryker configs run and are measured (round-21 T3, `Issues/ledgers/T3-mutation-survivors.md`): generator 21:02 @ 71.64% → `break: 71`; doctooling first measured 83.10 → 83, then **re-measured 67.96 → `break: 67`** after H1 below exposed 43 kills as artifacts of the run destroying its own comparison corpus. Generator's 71 is pending the same re-validation (H5). | CF §4.2, R21 T3 |
| C4 | `DONE` | `docs/research/testing-conformance-*` claimed several things that are no longer true, not the one line this row filed. **Scope widened at the final review**: the "Mutation testing — none" claim appeared in the REPORT's gap list, its per-category list, its gap table and its adopt-list, and again in the RESEARCH document's table — five places, all corrected to the adopted nightly leg. The Roslyn-floor claim was stale twice over (the pin is **5.0.0**, not 4.14.0, and a `roslyn-forward-compat` CI leg now exists) and is corrected in both places. Fixing the one filed line would have left four siblings shipping false. | CF §5b.2, final review |
| C5 | `DONE` | **(Closed 2026-08-22, round 22 W6.)** Both remainders routed to the shared helpers. (1) The `NoSuchSite` ratchet open-coded `AssertExactPin`'s two asserts per cause — by then it was `AssertExactPin`, not `AssertRatchet`, because B6 had already made it per-cause and exact; it now CALLS the helper, with the per-cause reasoning and the full breakdown passed in as `howToClose` so no message text is lost. (2) `AssemblyScanTests`' private repo-root walk and its private source enumerator are gone; every path goes through `RepoPaths`, whose walk looks for `DwarfMapper.NET.sln` — a real file in a git WORKTREE as well as in a plain clone, unlike `.git`. Re-measured across the switch: 56 generator sources and 443 test sources either way, so the scanned corpora are unchanged. The `ci.yml` counts were corrected at the final fix wave, as the row already recorded. | CF §5b.6 |
| C6 | `DONE` | **(Closed 2026-08-21, round 21 T7 — `c613e7f`.)** All three named survivors are Killed in the re-measured runtime leg (`StrykerOutput/2026-08-21.19-37-59`): the `Register` duplicate → `InterfaceMaps` mirror invariant is pinned by `RegistryInterfaceLookupTests.A_duplicate_interface_registration_is_marked_but_does_not_poison_lookup` (a duplicate interface registration is marked ambiguous yet still resolves with the first delegate through ONE mirror entry); the depth-exception ctor/message and the `FormatMessage` iterator-vs-declare fork are pinned by `DwarfExceptionContractTests` (killedBy confirmed per mutant in the JSON). Runtime leg re-measured **87.61 %** (99 killed of 113 scoreable; 12 survived, 2 no-coverage, **0 timeouts**), 11 min 36 s → `break` 61 → **87** in the same commit, `low` raised with it (Stryker refuses `low < break` with exit 0, H1). `:291` (`Key.Equals` `&&`→`\|\|`, L282 at this measurement) **survives deliberately** — ruled equivalent in practice per CF §5.4, left unchased. T4's deferred bullet landed with it: `RegistryPropertyTests` (CsCheck 4.7.0; round-trip, duplicate first-wins+marking, ordered-key identity + table separation; `DeepPopulation.RegistryPropertyIters` fast 200 / deep ×10). Remaining 12 survivors + 2 no-coverage are E3-E1's holes 2–3 (null-guard statement removals on `Update` L258–261 / `Map` L133–134), 6 (the ruled `Key.Equals`), 11 partially (4 of its 16 `FormatMessage` mutants: three remedy string tails plus the `Count: > 1` boundary on an input `Map` can never produce) and 13 (the facade `TryGet` guard), plus no-coverage holes 7 (`Equals(object)`) and 10 (`TryEnterNode`); holes 1 (`RegisterUpdate` guards) and 5 (`Key.GetHashCode` mixing) are CLOSED in this measurement. | CF §5.3–5.4, R21 T7 |

## D. Decisions — **ruled**, now ordinary work

These were parked awaiting a maintainer. Under the execution rule adopted 2026-08-16 they were ruled on
instead, each with what it costs if the ruling is wrong. **Every one is reversible in a single commit.**
Full reasoning is in the ledger under `Ruling:`.

| # | Status | Ruling | Cost if wrong |
|---|---|---|---|
| D-a | `TODO` | **`NullCollections`@`Projection`: keep today's behaviour, keep the divergence entry, and document it** in `docs/options.md` as *projection's collection null-semantics are `AsNull` by nature*. Option (a) risks failing inside a translated query at runtime — worse than a documented divergence. Option (b) was implemented and reverted after breaking seven tests. | Nothing changes at runtime; the entry stays recorded and (a)/(b) remain open to a later maintainer. |
| D-b | `DONE` | **Delete `ResetForTests`.** Landed at the final fix wave, not when it was ruled: round 19 had *extended* it instead, and the final review found both halves of the inconsistency independently. **Delete**,  Zero callers; its IVT targets a project whose registry tests do not use it; the mutation leg excludes that project — so round-19's additions to it are unverifiable dead code *by construction*. | A future test wants a reset hook and re-adds ~8 lines. |
| D-c | `TODO` | **Delete the stale items from `CLAUDE.md`'s working note** — the file's own rule is to delete each once decided, and one describes a fence-allowlist mechanism that no longer exists. ⚠️ This edits the agent's own instructions, so it is called out rather than done quietly. | Two historical notes lost — both preserved here and in `CARRY-FORWARD.md`. |
| D-d | `DONE` | **Keep `internal` + `[InternalsVisibleTo]`.** The four meta-attributes need it; public would grow the shipped API for test-only metadata, and a separate assembly breaks the single-package delivery story. The CRA objection was about what the IVT *exposes* — **D-b resolves it**, leaving only inert metadata with zero runtime reads. | If the CRA posture later demands zero IVT, the meta-attributes move to their own assembly — a contained refactor. |
| D-e | `DONE` | **All 76 pre-CHANGELOG diagnostics announced** (2026-08-21, maintainer-approved): written as the "initial diagnostic surface" block under `CHANGELOG.md` `### Added`, one entry per id derived from `docs/diagnostics.md`; the drained `PredatesTheChangelog` baseline was deleted, so `Scan9` now guards every live id with no exemptions (sabotage-verified: deleting one entry turns the suite red naming the id). | Nothing — the first release notes are complete and the gate is uniform. |
| D-f | `DECIDE` | **Should `[MapTo]` types participate in ambient registration at all?** Exposed when **D17**'s stated justification was falsified at A5 and never filed anywhere until now. The `[MapTo]` front door emits an extension class and **no** ambient registry rows, so `RegisterCollectionShapes` has nothing there to withhold — the option A5 was asked to thread has no subject. That is either a deliberate scoping of ambient registration to the `[DwarfMapper]` front door, or an omission nobody has decided about. A12 item 4 reclassified D17 as structural **on today's answer**, which is honest and reversible, but it does not settle the question. | Answering it either way is a bounded change to one front door; leaving it unanswered means the structural reclassification rests on an accident. |

## E. Research — measure before changing anything

| # | Status | Task |
|---|---|---|
| E1 | `DONE` | **Mutation leg costs 44 min because 91 tests do all the killing and 5,592 pay for it** (~61× waste). Measured, and the premise was **half wrong in a way worth keeping**. The scoped leg is real — 5,592 → 288 tests, 44:02 → **02:50**, same 111 mutants — but the 44:02 baseline was taken under concurrent builds, and the full unscoped suite re-timed on a quiet machine is **12:25**. Contention, not test count, was most of the gap. The committed config therefore retains **no exclusion**, per the maintainer ruling; the leg is CI-able either way. Two findings falsified earlier approved work: `test-projects` is **inert** in Stryker 4.16 (so Task 9's exclusion never took effect and its "measured cost is zero" was false), and `test-case-filter` **does** exist despite being absent from `--help`. `break` accepted at **61**: no kill was lost — all seven losses are `Timeout → Survived` and the 66.95% was inflated by bail-on-by-default. |
| E2 | `DONE` | **Time the torture collection × 49** — measured 2026-08-21 (Release, `--no-build`, quiet machine, 3 runs): the `registry-torture` collection (13 tests: RegistryConcurrencyTortureTests + RegistryUpdateContractTests) costs **1.04 s in-test** (trx per-test sum), 1–2 s reported run, **3.2/3.7/4.2 s command wall** (median 3.7 s incl. host start, which Stryker pays separately per mutant). **× 49 registry mutants = ~51 s in-test (≈1.9% of the 2,642 s run), ~181 s even charging full command wall (≈6.9%)** — R21-3's "at even 10 s per pass that is ~8 minutes" speculation is refuted: a pass is ~1 s, and hypothesis (2) is a *minor* co-cause, not a third of the run. The 60→240 round change needs no revisiting for the mutation leg; the covering-set explosion (E1) stays the whole story. Deep tier (×4 rounds, `DWARF_DEEP=1`): 3.6/4.6/4.6 s in-test, 6.3/6.7/6.7 s wall — still trivial. |
| E3 | `DONE` | Check from the existing JSON whether any mutant was killed **only** by an accidental toucher — de-risks E1 with no run at all. **It did its job and saved E1**: filtering to the hand-written list of 7 classes would have lost **18 killed mutants** (66.95% → 51.69%). The list DERIVED from the data is 23 classes / 127 killer tests — 16 classes nobody had named, including `FacadeUpdateIntoTests`, `AmbientRegistryTests` and the whole preserve/graph family — and with it, zero kills lost. |
| E4 | `DONE` | Isolate the culture-swapping tests into their own collection so the rest of `IntegrationTests` can parallelise — done 2026-08-21 (round-21 T4 branch). The three culture-swapping classes (ConversionDefensive/ParsableConversion/SecurityRegression) moved to a serial `culture-swap` collection, the assembly-wide `DisableTestParallelization` removed, its replacement comment carries the expired-cost citation (R21-3). **The second hazard was audited, not hand-waved:** every registering test outside the serial `registry-torture` collection registers its own distinct closed types and asserts per-key; the single `Provided` read outside it (`AmbientRegistryTests`) is `Assert.Contains` of the test's own key — no test asserts registry-GLOBAL state from a parallel collection (audit recorded in `AssemblyInfo.cs`). Measured (3 runs each): assembly median **3.91 s → 3.50 s** (~10%, honestly near noise at suite scale — the payoff is correctness isolation plus once-per-mutant multiplication under Stryker). 3 full-suite runs + 1 deep run after: **0 flakes exposed** — no new H-rows from re-enabled parallelism. |

## F. Branch and process — the work that is not a code fix

Previously invisible: it lived only in the SDD ledger and in my head. It belongs here like everything else.

| # | Status | Task |
|---|---|---|
| F1 | `DONE` | **(Closed by reality; recorded 2026-08-22, round 22 W6.)** Merged at **`dc385d4`** — *"Merge feat/surface-coverage-architecture: rounds 19 and 20 (F1, maintainer-approved)"* — and on `master` since; round 21 merged on top of it at `d131c76`. Original: **Merge `feat/surface-coverage-architecture`.** Round 19's final whole-branch review returned *merge after must-fixes*; those were A0 and are done. **Needs your say-so — a merge is a side effect outside this worktree, so I do not do it on a ruling.** 30 commits, `+6.4k/−0.5k`, of which `src/` is only +729. |
| F2 | `DONE` | **(Closed 2026-08-22, round 22 W6.)** The worktree is gone — `git worktree list` shows only `DwarfMapper.NET` (master) and the current round-22 one — and the ledger was captured BEFORE the deletion, which was the whole condition of this row: `656042c` *"commit the SDD ledgers — they hold ~20 rulings and were git-ignored"* put them under `Issues/ledgers/` (`round20-ledger.md`, and `A7-notes.md` at `c8ba8bb`). Round 21 followed the same rule at `96e62f9`. The practice is now standing: capture the ledger in the repo before the worktree is removed. Original: Decide the fate of the worktree after merge. It is a real git worktree at a sibling path, plus a git-ignored `.superpowers/sdd/` workspace holding the ledger, briefs and reports. The ledger is the only record of ~15 rulings; **capture it before deleting anything.** |
| F3 | `TODO` | Round-19 plan and spec live in `docs/superpowers/`; round-20's plan is on the branch. If the branch merges, three plan documents land in `docs/` — decide whether they stay as history or move under `Issues/`. |
| F4 | `DONE` | Pre-flight conflict scan for the round-20 plan (table + four ordering rulings) — recorded in the ledger. Was skipped before A0 and run late. |

## G. Every machine that holds work and executes steps

The inventory. Each of these holds a list and drains it — and until now most existed only implicitly, which is
how the same item ended up recorded in three places and the ceilings ended up misquoted in a fourth.

| Machine | Where | Holds | Drained by |
|---|---|---|---|
| **Round-20 task list** | `Issues/round20/TASKS.md` | this file — ~40 items | the standing rule: every issue lands here |
| **Divergence store** | `Contracts/DeclaredDivergences.cs` | **2 findings / 4 cells**, each asserting its defect *still exists* | section **A** — closed; both survivors are maintainer-acknowledged design decisions |
| ~~**`PredatesTheChangelog`**~~ | *deleted* | held **76** diagnostics never announced | **D-e** — DRAINED and the store DELETED 2026-08-21; `Scan9` now guards every live id uniformly, with no exemptions |
| **Mutation survivors** | `StrykerOutput/…/mutation-report.json` | re-measured round 22: runtime **96.46 %** (P2, `b74023f`), doctooling **95.42 %** (P3, `254c500`), generator **81.59 %** (P5, `270d5cf`); the three `break` values moved to those measurements in the same commits. Was 66.95 % / 39 survivors when round 20 began | **C6** done; **P2/P3/P5** are the round-22 kill lists; the remainder is catalogued in `Issues/ledgers/` |
| **Round-19 SDD workspace** | `.superpowers/sdd/2026-08-13-…/` | ledger + briefs + reports, **git-ignored** | complete; captured into `Issues/ledgers/` at `656042c` before the worktree went (**F2**) |
| **Round-20 SDD workspace** | `.superpowers/sdd/2026-08-16-…/` | ledger + briefs + reports, **git-ignored** | complete; captured as `Issues/ledgers/round20-ledger.md` (`656042c`) |
| **Round-21 / round-22 SDD workspaces** | `.superpowers/sdd/2026-08-2…/` | ledger + briefs + reports, **git-ignored** | round 21 captured at `96e62f9`; round 22 in use |
| **Worktrees** | `DwarfMapper.NET` (master), `DwarfMapper-r22` (`feat/round22-gates`) | the surface worktree is gone: its ~106 commits merged at `dc385d4`, round 21's at `d131c76`. Round 22's count moves daily — re-run `git rev-list --count master..HEAD` rather than trusting a digit written here | **F1**/**F2** done; the round-22 merge is the maintainer's |
| **CI** | `.github/workflows/ci.yml`, `release.yml` | build/test legs incl. the `SurfaceMatrix` trait leg **and the mutation leg** (nightly cron + `workflow_dispatch`, merged at `d2d54cf`, dormant until master) | **C1** — done; **C5** — the cell counts in `ci.yml` are corrected at the final fix wave |
| **Scripts** | `scripts/` — `housekeeping.ps1`, `mutation-battery.sh`, `conformance-gate.sh`, `run-aot-bench.ps1`, `git-hooks/` | the `-Mutation` legs and the non-vacuity guard | **C1**, **C3** |
| **Stryker configs** | `stryker-config{,.doctooling,.runtime}.json` | three mutation legs, all three run and measured | **C3** done; round-22 P2/P3/P5 re-measured every `break` (96 / 95 / 81) and `RatchetInvariantScanTests` (P1) now asserts each one equals a dated measurement in its own comment |
| **Research** | `Issues/round21/RESEARCH.md` | 3 items, one already measured | section **E** |
| **Reasoning archive** | `Issues/round20/CARRY-FORWARD.md` | the *why* behind these items | not a worklist — do not track work there |

### The eleven shrink-only ratchets

Each is a population that may only get smaller. **No task may lower one it did not re-measure in the same
commit** — predicted movement has proven unreliable (A1 moved a cell between two `NotCompilable`
sub-populations and no ceiling changed at all).

Values below are read from the code at branch HEAD. `EmittedInvalidCode` is the eleventh, added by **A12
item 5**; the table used to be titled "the ten" and did not list it.

| Ratchet | Value | Meaning |
|---|---:|---|
| `NoSuchSiteCellCeiling` | 116 | no declaration site exists — wholly structural since **A11** closed the template gap |
| `NotCompilableCellCeiling` | 0 | compiler rejected the placement. **Zero, and still reachable and pinned** — `Refused` is now asked first (**A10**), and what was left over was the generator's fault, not the placement's (**A12 item 5**) |
| `EmittedInvalidCodeCellCeiling` | 0 | **the generator emitted code that does not compile.** The population this repository says must not exist is EMPTY: B33's 2 cells closed by correct emission and B27's 8 by `DWARF094` (round 21 T6, 10 → 8 → 0, each re-measured in its own commit) |
| `UnaskableCellCeiling` | 44 | the case-space cannot pose a question |
| `UnhonouredButLoudCellCeiling` | 14 | changed nothing, but the build fails anyway |
| `DivergentCellCeiling` | 4 | cells covered by a recorded divergence |
| `DivergenceFindingCeiling` | 2 | recorded divergences — `NullCollections`@`Projection` and `MaxDepth`@`Span`/`AsyncStream` |
| `StructurallyExcusedCellCeiling` | 13 | shape-based, not behavioural. The 12 → 13 raise is **the one sanctioned raise of the round** (A12 item 4, D17) |
| `PredatesTheChangelog` | 76 | diagnostics never announced — **D-e**, needs a human before the first tag |
| `DirectCompileErrorCallBaseline` | 53 | direct `RunAndGetCompilationErrors` call sites |
| `MapMethodModelBoolFlagBaseline` | 15 | signature-triggered map modes |

**Four of these are pinned EXACTLY rather than ratcheted**, as of the final fix wave:
`NotCompilable` (0), `DivergenceFinding` (2), `DivergentCell` (4) and `EmittedInvalidCode` (0). The shared
`AssertRatchet` helper's shrink side is a ten-wide tolerance band, which for a population of ten or fewer
passes every count from zero upward — so closing one cell silently funded a replacement of the same shape,
and the acute case was `EmittedInvalidCode`: fixing B27's eight cells would have let eight brand-new
broken-emission defects land green. `AssertRatchet` now refuses a ceiling of ten or less outright.

---

## H. Round 21 — found while executing the depth-tier plan (`docs/superpowers/plans/2026-08-19-round21-depth-tier.md`)

| # | Status | Task |
|---|---|---|
| H1 | `DONE` | **Running a doc-adjacent mutation leg destroyed real repo files** — README.md, CONTRIBUTING.md, docs/diagnostics.md, 2,523 lines (reverted, nothing lost). Verified mechanism, which falsified the first hypothesis: there is no Stryker sandbox to escape — Stryker 4.16 swaps the mutated DLL into the test project's **real `bin/`** (leaving `*.dll.stryker-unchanged` beside it), so the doc tests' heal-or-fail write-back wrote a *mutated renderer's* output over the real docs. Fixed at `3982f25`: `Contracts/RepoWriteGuard.cs` centralizes refusal (detects the `.stryker-unchanged` marker / `StrykerOutput` ancestors), all four repo writers rewired through it, tests still run and assert under mutation (no-exclusions ruling honoured), pinned non-vacuously plus an ARCH-06 scan requiring every raw file-write in `tests/` to be registered. This is round 19's ARCH-06 made concrete. |
| H2 | `DONE` | **SonarAnalyzer.CSharp license ruling — needs you.** Its symbolic-execution rules (S2583/S2259/S3966) are the one unique catch left on the table, but v10.x is the *Sonar Source-Available License v1.0* (non-OSI; the LGPL line froze at 9.x). Build-time-only, never distributed — but this repo documents a license stance per dependency, so adopting non-OSI silently was not the executor's call. Options: accept SSAL for a deep-tier-only conditional reference (`-p:RunSonar=true`), pin aging LGPL 9.x, or drop. **RULED 2026-08-21: REJECTED outright** — the maintainer cited SSAL's key rules as disqualifying: the no-unapproved-AI clause (this repo is AI-developed), derivative-works-must-be-public, no-competing-services, notice obligations. LGPL 9.x pin declined too (frozen line). Symbolic-execution gap stays covered by CodeQL + the round-22 differential/metamorphic arc. Full ruling: `Issues/round22/RESEARCH-97-PERCENT-GATES.md` (master, 1c1c991). |
| H3 | `TODO` | **Meziantou phase 2**: extend from the five `src/` projects to tests/samples/benchmarks. Deferred from T1 — est. one session of noise triage across 11 test projects; keeps the repo's "analyzers everywhere" posture consistent. |
| H4 | `DONE` | **A mis-ordered Stryker config disables the leg silently**: `break` &gt; `low` makes Stryker refuse to evaluate **and exit 0** — the doctooling leg was unrunnable from T3's config until H1's fix and nothing noticed. Extend the non-vacuity guard (`Assert-MutantsWereTested`) / housekeeping to assert every leg produced a scored report, and add a config sanity check `break ≤ low`. **Closed by T5 (2026-08-21):** `Assert-StrykerConfigSane` in `scripts/housekeeping.ps1` checks `break ≤ low` for all three configs up front (before leg 1, so a leg-3 mistake cannot cost half an hour first), mirrored as the pre-tool-install "Assert the config is runnable" step of the CI `mutation` matrix job; and every leg — local and CI — now ends in the non-vacuity guard (`Assert-MutantsWereTested` locally; the inline mirror per matrix leg in CI), which fails on a missing report as well as on 0 scoreable mutants. A silently-refusing Stryker (exit 0) therefore fails twice: before the run on the config's shape, after it on the absent scored report. |
| H5 | `DONE` | **Re-validate the generator leg's `break: 71` under the H1 guard.** The doctooling baseline lost 15 points when the self-corruption artifacts were removed; the generator leg is exposed to the same class (a mutated *generator* changes diagnostics-index input, doc tests heal-or-fail, later mutants compare against altered docs). One quiet-machine re-run; set break to the measured floor in the same commit. **Result: 471 mutants, zero status flips vs pre-guard, score 71.64% identical, Timeout 0, 19:41 wall-clock — `break: 71` validated, unchanged; the artifact class was DocTooling-only.** |
| H6 | `NOTE` | **Deep-tier budget**: runtime 12:25 + generator 21:02 + doctooling ~5:30 ≈ **39 min of the 44-min target** before coverage/fuzz/benchmarks. Measured cause: 85 of the generator's 201 scoreable mutants are `static`, each re-running the whole suite. This is T5's central trade-off; `mutate` was not narrowed and no tests were excluded. **The 44-min arbitration is SUPERSEDED (2026-08-21)** by the maintainer's raised-ceiling ruling (recorded on master in `Issues/round22/RESEARCH-97-PERCENT-GATES.md`, "Maintainer rulings", commit `3a3dff8` — cited, not on this branch): ALL legs run EVERY night, no rotation, no total-duration gate — record the measured total instead. T5 wired it: the nightly is the `mutation` matrix (runtime 11:36 + generator 19:41 + doctooling ~5:30 ≈ **37 min aggregate**, all at current measurements) plus `deep-test` (deep suite ~1:45 at 15,786 tests, coverage collection ~+0:18 and ReportGenerator ~0:15, ILVerify ~1 s ≈ **~2:15**), ≈ **39-40 min measured compute** before per-job restore/build/tool-install overhead (~3-5 min × 4 nightly jobs on hosted runners). The jobs run in parallel on separate runners, so nightly **wall-clock ≈ the slowest leg (generator)**, not the sum. The plan's "fail if total &gt; 44 min" was not implemented — deliberately. |
| H7 | `DONE` | **Timeout mutants: dissect and kill deterministically** (maintainer directive 2026-08-19). A `Timeout` kill is a wall-clock oracle over nanosecond-scale operations — nondeterministic by construction, and already the cause of three measurement distortions (runtime 66.95→61, generator run-1 inflation, the quiet-machine ritual itself). For EVERY mutant Stryker classifies `Timeout` in any leg: (1) identify the mutation and reason about *why* it makes the loop non-terminating — name the broken termination/progress invariant; (2) write a deterministic test that pins that invariant (bounded iteration, progress assertion, postcondition) so the mutant dies as `Killed`, not `Timeout`; (3) where the hang exposes a production loop whose termination hangs on a single fragile expression, judge whether the loop itself deserves hardening — as a robustness fix on its own merits, never as mutation-appeasement; (4) tests that can hang forever under a defect are bad diagnostics — bound them. Exit criterion: `Timeout` count 0 across all three legs, scores no longer load-sensitive. **Met 2026-08-21:** phase 1 dissected every Timeout ever recorded (`Issues/ledgers/H7-timeout-dissection.md`) — only three ever genuinely hung, all DocTooling; phase 2 killed their classes structurally (progress guard in `DocSnippetInjector.Inject`, `Dedent` trim restructured to `FindIndex`/`GetRange`) with sabotage-demoed fast-fail tests, and the DocTooling re-measure landed **Timeout 0** (193 Killed / 54 Survived / 37 NoCoverage, 67.96 %, `break: 67` re-affirmed, 5:04). Generator leg measured Timeout 0 at H5 (zero flips); the runtime leg's lone historical Timeout is dissected as clock noise on a mutant that cannot hang, resolved by its `additional-timeout`. No detection anywhere now rides on a wall clock. The dissection's §5 item 4 — the runner-level `--blame-hang` backstop — landed in T5 (2026-08-21): `--blame-hang --blame-hang-timeout 5m` on every plain `dotnet test` step (CI's build-test / surface-matrix / roslyn-forward-compat, and housekeeping's suite stage, which the nightly `deep-test` job runs); the Stryker legs deliberately excluded, their per-mutant ceiling plays that role. |
| H8 | `TODO` | **`scripts/housekeeping.ps1` was unrunnable on this machine — no `pwsh` existed, and Windows PowerShell 5.1 cannot even PARSE the file** (found at T2, 2026-08-21). The script's shebang targets `pwsh`, but nothing on the machine provided it, and 5.1 reads the UTF-8-without-BOM file as ANSI: the em-dash `—` (E2 80 94) decodes under cp1250 so that its last byte becomes U+201D, a *closing smart quote*, which PowerShell honours as a real quote delimiter — the file dissolves into parse errors. Every housekeeping gate (mutation legs, now coverage floors and ILVerify) was therefore only ever runnable in an environment someone hand-built. Resolved locally by `dotnet tool install -g powershell` (pwsh 7.6.5, on `~/.dotnet/tools` like the repo's other tools). Decide the durable form: document the pwsh prerequisite (plus `dotnet-reportgenerator-globaltool` and `dotnet-ilverify`, which `-Coverage`/`-ILVerify` need) where a cold reader finds it, and/or add a UTF-8 BOM so 5.1 at least parses the file instead of failing cryptically. |

## I. Round 22 — found while executing the round-22 plan (`docs/superpowers/plans/2026-08-21-round22-compiler-testing-and-97-gates.md`)

| # | Status | Task |
|---|---|---|
| I1 | `TODO` | **`Key.Equals(object)` is honestly uncoverable — needs your ruling** (found at P2, 2026-08-21; E3-E1 hole 7, the runtime leg's last NoCoverage mutant). `DwarfMapperRegistry.Key` is a *private* nested struct; `ConcurrentDictionary` resolves equality through `IEquatable<Key>.Equals`, so the `object`-typed override is reachable only by boxing a `Key` — which no public surface does, and a test could only do via reflection, which the house accessibility stance forbids. P2's rule ("killable only via product change: stop and file") applies. Two candidate dispositions, both yours: (a) the one-word `private` → `internal` on the nested struct (`InternalsVisibleTo("DwarfMapper.Generator.Tests")` already exists — the same internal-plus-IVT pattern the constructor stance prefers over reflection) plus a contract test pinning equal pair → `true`, half-matching pair → `false`, non-`Key` object → `false`, `null` → `false`; or (b) a denominator ruling in the spirit of D-b's `ResetForTests` deletion — though the override cannot simply be deleted (CA1067 demands `Equals(object)` when `IEquatable<T>` is implemented). Until ruled, the mutant stays NoCoverage and is priced into the leg's measured score; it is deliberately NOT in the equivalents ledger (an uncovered override is a coverage question, not an equivalence). Full analysis: `Issues/ledgers/E3-E1-report.md`, round-22 P2 appendix. |
| I2 | `TODO` | **Three DocTooling filesystem-composition-root mutants are honestly unkillable test-side — seam decision is yours** (found at P3, 2026-08-22; the DocTooling leg's last non-adjudicated survivors). `SnippetScanner.ScanAll`'s `OrderBy → OrderByDescending` and `"*.cs" → ""`, and `ExampleCatalogue.Scan`'s `"*.cs" → ""`, all enumerate the REAL `samples/` tree via `RepoLayout`. They are corpus holes, NOT equivalents (an empty `GetFiles` pattern returns ALL files — measured 105 vs 44 under the Gallery — and a duplicate id split across files would make scan order observable), but killing them requires planting files in the real tree (banned; RepoWriteGuard exists to stop exactly that) or redirecting `RepoLayout.Root` (banned; a redirect changes what the leg proves). The honest fix is the seam `Build` already has: the composition roots taking an injectable file list, with the current `Directory.GetFiles` call reduced to a one-line default argument — a product-shape decision, so filed rather than done. Until ruled, the three stay named Survived and are priced into the leg's measured 95.42 % (`break` 95); they are deliberately NOT in the equivalents ledger. Full analysis: `Issues/ledgers/T3-mutation-survivors.md`, P3 section. |
| I3 | `TODO` | **`MapToAttribute` is the concrete research-Q3 exhibit — needs your category ruling** (found at P4, 2026-08-22; the runtime assembly's one remaining 0%-covered class, 0/4 lines). It is a compile-time-only attribute like the nine P4 excluded, BUT its ctor carries `targets ?? Array.Empty<Type>()` — a defensive arm no caller can trigger (C# `params` never passes null for `[MapTo(typeof(A))]`; only an explicit `new MapToAttribute(null!)` could, which nothing does). That arm is exactly research Q3's *defensive unreachable arm* category, which the plan pre-declares "holes by definition until ruled" — so P4 left the class in the denominator at 0/4 rather than smuggle an unruled category in under the attribute one (cost: runtime 91.2 instead of 92.4). Your options: (a) admit the Q3 defensive-arm category (scan gains it as a second sanctioned string, pin 9→10 same commit); (b) delete the `??` arm — `params Type[]` is never null from attribute syntax, making the class a clean fit for the EXISTING category (nullable annotations make the `null!` misuse a caller bug); or (c) leave it — 4 lines of honest hole, priced into the 91.2 floor. Classification table and arithmetic: `.superpowers/sdd/2026-08-21-round22/progress.md` P4 section; the scan's pin comment (`RatchetInvariantScanTests`) names the same exhibit. |
| I4 | `TODO` | **The generator Stryker leg plants MUTATED `DwarfMapper.Generator.dll` copies in analyzer-referencing test bins with NO backup marker — the decontamination sweep must widen** (found at P5, 2026-08-22). After the clean `01-14-14` run, six test bins (`ConsumerTests/CleanCorpus`, `ConsumerTests/Host`, `CorpusTests`, `DifferentialTests`, `IntegrationTests`, `Testing.Tests`) each carried a `DwarfMapper.Generator.dll` string-matching "Stryker", timestamped inside the run's mutate/compile phase, with no `*.stryker-unchanged` beside it — Stryker backs up only files it OVERWRITES, and these bins had no pre-run copy (analyzer-only reference, no CopyLocal), so restore never touched them and `RepoWriteGuard`'s leftover-backup signal cannot fire there either. Hazard: an incremental build keeps the newer mutant, and those suites then exercise a mutated generator silently green. P5 swept them (deleted; suite 7,756/0 proves absence is the correct state — matching a fresh build). The H1/P2/P3 closeout sweeps checked only the leg's own target DLL beside backups; the honest sweep is *every* `DwarfMapper*.dll` under `tests/**/bin` string-scanned against the src-built originals after ANY leg. Decision yours: fold that sweep into `scripts/housekeeping.ps1 -Mutation` (it already owns Assert-MutantsWereTested), or extend `RepoWriteGuard`'s detection — the former is where the precedent (NOTE 3, T3-H1) lives. |

| I5 | `TODO` | **Synthesized collection/dictionary element maps do not lift over `Nullable<T>` — silent CS1503 in generated code, the `EmittedInvalidCode` genre** (found at K0, 2026-08-22, by the type-graph smoke's FIRST 25-sample run; CsCheck-shrunk, then scoped by a 12-cell probe). Shape: `List<S?> → List<D?>` (or any supported wrapper — List, `T[]`, IReadOnlyList, HashSet, Dictionary value all measured diverging) where the element pair `S`→`D` needs a synthesized element map and the SOURCE element `S` is a struct/record struct. The emitted helper loop calls `__DwarfMap_Obj_S_D(__item)` with `__item` of type `S?` — CS1503, generator silent. Measured boundaries: class elements fine (`?` is annotation-only), non-nullable struct elements fine, non-collection nullable nested struct members fine (`S? → D?` plain member handles the lift), struct-source×class-dest diverges, class-source×struct-dest does not (implicit `D → D?` covers the write side). Fix direction is yours: unwrap-with-null-policy in the element loop (what does `null` element become — the NullStrategy family question), or a loud refusal (DWARF027-adjacent) until ruled. Pinned: `PinnedCorpus.NullableStructElementMap` (`I5-nullable-struct-element-map`, tests/DwarfMapper.CompilerTests) asserts EXACTLY this divergence and goes red on the fix; the matching sampled-space exclusion in `TypeGraphGen.Assemble` is keyed to this row and must be deleted in the fixing commit. Five-file sync applies if the fix adds a diagnostic. |

| I6 | `TODO` | **The local housekeeping AOT stage (3/4) fails with NETSDK1207 under the pinned SDK 10.0.101 — pre-existing, NOT round-22 work, and the repo already documents the exact cause** (found at K0's closeout verification, 2026-08-22, the first full `housekeeping.ps1 -Coverage` pipeline run this round). `scripts/housekeeping.ps1` L312 runs `dotnet publish samples/DwarfMapper.AotBench … -p:PublishAot=true`, which fails with NETSDK1207 ("AOT compilation is not supported for the target framework") against `src/DwarfMapper.Generator.csproj` and `src/DwarfMapper.CodeFixes.csproj` — the two netstandard2.0 projects in the publish graph. Reproduced IDENTICALLY at the round-22 branch tip and at the branch base `73c58c3` on the master checkout: no round-22 commit is the cause. The mechanism is the one `.github/workflows/ci.yml`'s own aot-trim-gate comment (L362-363) states verbatim — "PublishAot is deliberately NOT passed on the command line… `-p:` sets a GLOBAL property, which MSBuild flows down the entire graph" — and CI therefore relies on the csproj-level `<PublishAot>true</PublishAot>`, which **AotBench's csproj also already has (L5)**. So the fix is one line, yours to bless: delete `-p:PublishAot=true` from housekeeping L312 (the csproj property already makes the publish AOT; the global-property flow into the netstandard2.0 references is the only thing the flag adds). Why it surfaces now: likely SDK-side tightening of the NETSDK1207 check; the ISSUE-038 pin (10.0.101, rollForward disable) landed 2026-08-11 and no round-21/22 ledger records this stage green since — the round-21/22 verification runs all exercised the suite/coverage/mutation stages, not stage 3. Stages 1/1b/2 of the same run all passed (suite green under collection, coverage floors within band, exhaustion 766/766 in 10:35) — only stage 3 breaks. |

| I7 | `TODO` | **Mapping a null across a RE-KINDED nested pair throws at runtime instead of lifting `null` → `null`, although the destination member is nullable-capable — undocumented, I5's runtime sibling** (found at K1, 2026-08-22, by the differential oracle's FIRST 1,000-sample deep run, CsCheck seed `0vihQF5Vee7b`; minimized by a 6-cell kind-pair probe). Shape: plain (non-collection) nullable nested member `S1? M0_0` → `D1? M0_0` where the pair is CROSS-KIND. Measured table: Struct→Struct lifts null→null (the NullableProject ternary), Class→Class propagates null→null, but Struct→Class, Struct→Record and RecordStruct→Class all emit `s.M0_0 ?? throw new InvalidOperationException("Source member 'M0_0' was null")`, and the reverse Class→Struct throws `"Cannot map a null 'global::T.S1' to value-type 'global::T.D1'."` — in every throwing case the destination (`D1?` reference, or `Nullable<D1>`) could hold the null, and the lossless emission exists next door (MapEmitter's NullableProject path, gated on BOTH sides being `Nullable<T>`). Not covered by the documented `NullStrategy` sentence (docs/options.md: "Nullable-value source → **non-nullable** target when null: Throw") — these targets are nullable, so the throw is an undocumented semantic; per the K1 discipline the oracle was NOT taught it. Same root family as I5 (Nullable<T> lift gaps around synthesized pair maps): decide once for both — lift (`HasValue ? Helper(v.Value) : null` generalized over re-kinded pairs and reference sources), or a documented NullStrategy extension, or a loud refusal. Pinned: corpus rows `I7-nullable-rekind-value-to-reference` / `I7-nullable-rekind-reference-to-value` + `DifferentialOracleTests.I7_pinned_runtime_divergence_null_across_rekinded_pair_throws` assert the exact current throw messages and go red on any fix; the matching sampled-space exclusion in `TypeGraphGen.Assemble` (plain member, value-kind source × reference-kind dest) is keyed to those pins and must be deleted in the fixing commit. The reverse genre is unreachable in sampling only because the oracle population never nulls reference members (declared bias, `ReflectionOracle` header) — its pin is the deterministic executor. |

| I8 | `TODO` | **The one live NuGet advisory — NU1903 High, AutoMapper 14.0.0, GHSA-rvv3-g6hj-g44x — has NO license-compatible fix version; the standing per-project suppressions are the disposition, recorded here so the audit gate's one exception stays visible** (found at S1, 2026-08-22, the first restore under `NuGetAudit=all`/level `low`; also the only hit of `dotnet list package --vulnerable --include-transitive` across all 22 projects). Affected ranges `(, 15.1.1)` and `[16.0.0, 16.1.1)`; both fix versions (15.1.1, 16.1.1) are RPL-1.5 — GPL-incompatible per the pin comment in `Directory.Packages.props` — so no bump exists at ANY level, patch or otherwise (every GPL-compatible AutoMapper version is affected). It fires in exactly the three non-shipped projects that reference the benchmark competitor (`benchmarks/DwarfMapper.Benchmarks`, `tests/DwarfMapper.DifferentialTests`, `tests/DwarfMapper.ConsumerTests/CleanCorpus`), each carrying a pre-existing reasoned `NoWarn NU1903` (benchmark/test-only, IsPackable=false, no untrusted input; merged before S1 armed the explicit gate). S1 verified the gate has teeth behind the suppressions: stripping one → restore FAILS exit 1, `NU1903` as error under TreatWarningsAsErrors (demo reverted). No action needed unless you want to revisit the benchmark competitor set (dropping AutoMapper is the only way to clear the row); the suppression comments are the standing decision. |

| I9 | `TODO` | **Pair-scoped `[MapValue<TTarget>]` is not read at UpdateInto or Projection, and the diagnostic that says so names the wrong thing — needs your ruling on which half to fix** (found at W2, 2026-08-22, by B25's "unexamined claim" half, once the probe fix made the cells measure the directive instead of a type error). Measured: with the corrected probe, `[MapValue<Dst>("Name", "probe")]` reads `Refused — DWARF064 (Info)` at CreateMap, SpanMap, AsyncStream and CoLocatedHost (the shadow-info its non-generic twin also draws on the flat fixture), and `Refused — DWARF056 (Warning)` at **UpdateInto and Projection**. The cause is not a matching subtlety: `MatchPairValues(pairValues, …)` is consulted at exactly three sites — the create-map path (`MapperExtractor.cs:1077`), the `[GenerateMap]` pair path (`:1417`) and the nested-pair path (`:1607`) — and at neither the update-into nor the projection branch. So the pair IS mapped, by an update-into or projection method, and the directive simply is not read there; `DWARF056` nevertheless tells the caller it *"matches no mapped pair"*, which sends them looking for a missing `[GenerateMap]` that is not the problem. **Two candidate dispositions, and the choice is a design call, not a defect fix:** (a) *thread it* — read pair-scoped `[MapValue]` at UpdateInto and Projection the way the method-level form already is, which makes the two endpoints agree with create-map and is the D9-shaped answer; or (b) *re-attribute it* — keep the endpoints as they are and report `DWARF092` ("directive is not read at this mapping endpoint"), which is what the situation actually is and what `[FlattenGraph]` already reads at those same two endpoints. (a) changes emitted output for existing consumers and needs its own before/after measurement; (b) is wording plus a five-file-adjacent change and moves matrix readings. Recorded rather than guessed: B25's own text calls this an unexamined claim, and examining it is all this round's budget bought. | W2 (B25) |

## The honest caveat about this list

**Section A is closed** and section C is nearly so, which was the suggested cut line and it held. What remains
is section **B** — the test-infrastructure holes — plus the research and process rows. That is still a long
list, and the same risk applies: a list where everything is a task is a list nobody finishes.

**Where the next defect will sit, in the words of the final review:** *866/866 honestly judges what it judges,
but roughly 200 rows pass by being EXCUSED rather than by being RIGHT.* The fix wave tightened the four
smallest ratchets over those exemptions so churn below ten cells can no longer hide, and pinned the executed
cross-product's element count so a population cannot leave the matrix silently. **B3, B5, B6, B7 and B32 are
the remaining members of that family**, and they are the honest cut line now. *(Round-22 P6 update: B3 and
B6 closed 2026-08-22 — along with B11, every excuse category they named now carries a live, re-measured,
exactly counted obligation. B5, B7 and B32 remain.)*

---

## F1 — the merge handoff, written for the person who reads this cold tomorrow

**Everything below is in the repo so it survives a shutdown. Nothing is pushed. Nothing here needs me.**

### What you are deciding

**Merge `feat/surface-coverage-architecture` into `master`.** 113 commits, +19,786 / −894 across 143 files.
`master` has moved 4 commits since the fork point (`02d1a49`) — check those before merging; it is not a
fast-forward. The final whole-branch review (three parallel lenses: `src/`, `tests/`, docs/records/release)
returned **mergeable after one fix wave**, and that wave has landed and been re-reviewed.

| Verified at HEAD | |
|---|---|
| Surface matrix | **866/866 green** |
| Full solution tests | **7,657 passed / 0 failed**, 8 projects |
| Build | 0 warnings / 0 errors, samples included |
| Ceilings (11) | findings **2** · cells **4** · `NotCompilable` **0** · `EmittedInvalidCode` **10** · `NoSuchSite` 116 · `StructurallyExcused` 13 · `UnhonouredButLoud` 14 · `Unaskable` 44 · `PredatesTheChangelog` 76 · `DirectCompileErrorCallBaseline` 53 · `MapMethodModelBoolFlagBaseline` 15 |

### What ships to a consumer if you merge

- **Nine new diagnostics** (`DWARF087`–`DWARF093`, `DWARFR10`, `DWARFR11`); several are Errors that will
  break builds which currently compile. All in `CHANGELOG.md`.
- **One severity escalation:** `DWARF088` Warning → Error. Announced.
- **One BREAKING change:** `[MapTo]`'s generated extensions default to assembly-**internal**. Migration is
  `[assembly: DwarfMapperOptions(PublicExtensions = true)]`. Announced under BREAKING. Ruled in on your behalf
  because the option's own XML doc always documented that default and the registry contradicted it.
- **One new public member:** `DwarfMapperRegistry.IsUpdateAmbiguous`. Under `### Added`.
- **One public-behaviour fix:** `RegisterUpdate` no longer marks duplicates into the *create* table's
  ambiguity set (so `IsAmbiguous` stops returning `true` for pairs with no create map). Under `### Fixed`.
- **Three shipped defects fixed that no test or sample had reached:** `[AfterMap]` on `void Update(S,D)`
  emitting **infinite recursion**; duplicate `[FlattenGraph]` emitting `CS1912`; `[MapTo]` on a `struct`
  never compiling (`CS0037`, four inline null-guard sites in one file).

### The one thing that still needs you and is NOT in the merge

**D-e.** ~80 diagnostics predate `CHANGELOG.md` and have never been announced. `PredatesTheChangelog` holds
the 76 that remain; `Scan9` guards only *new* ids, so **nothing catches this before the first tag.** The fix
wave put a pointer on the release path (the CHANGELOG's release-cutting comment and `docs/RELEASING.md`), but
the entries themselves are release-notes writing, and yours.

### Rulings made on your behalf — 28, each reversible, each with what it costs if wrong

Full text with reasoning is in `Issues/ledgers/round20-ledger.md` (search `Ruling:`). The ones you should
actually look at, because they changed shipped behaviour or a claim:

1. **D18 breaking change kept** (registry extensions internal by default). Cost if wrong: one attribute per
   consuming library, found at compile time.
2. **`break: 61` accepted** for the runtime mutation leg over the previous 66. The 66 was inflated by
   load-induced timeouts on mutants that provably cannot hang, plus one equivalent mutant. Cost if wrong:
   ~6 points of real regression admitted; the config comment says 66 must not be restored.
3. **No test exclusion in the mutation leg** — your instruction, overriding my earlier filter. Full suite
   runs in 12m25s on a quiet machine, not 44 min; the original figure was contention. Nightly cron.
4. **D17 reclassified structural** with the one sanctioned `StructurallyExcused` raise 12→13. Its filed
   reason had been a pun on "registry"; the row now carries the A5 measurement.
5. **D-a `NullCollections`@Projection**: keep behaviour, keep the ratchet entry, document it. Options (a)/(b)
   remain open to you — (b) was tried and reverted after breaking seven tests.
6. **D-b delete `ResetForTests`** — ruled early, **implemented only in the final fix wave** after two reviewers
   found it undone. D-d (keep `internal`+IVT) rests on it; both now hold.
7. **`DWARF088` escalation moved to A12**, not A10, so a severity flip never shared a commit with a
   population reclassification.
8. **A6/A8/A11/A13 partials accepted** where the honest state was red — each measured, reverted, parked, and
   later landed. An honest red was chosen over a wrong green four separate times.

The remaining 20 are ordering, batching, scoping and process rulings; none changed shipped behaviour.

### What round 20 established that you should not lose

- **Nine round-19 findings had false filed evidence.** Four or more traced to a fixture too thin to pose the
  question. Round 19 found 23 cells worth investigating, not 23 defects; the instrument was sound, its
  inputs were not.
- **Nine defects in this round were "a guard exists on a sibling path and the new code did not inherit it."**
  Two shipped generator crashes; both caught at review. The eleven structural unifications are the answer,
  and the `src/` reviewer verified them behaviour-preserving *as a set*.
- **Eight vacuous mechanisms found**, none remaining after the `tests/` reviewer's hunt for a ninth.
- **The `tests/` reviewer's one line, verbatim:** *866/866 honestly judges what it judges, but roughly 200 rows
  pass by being excused rather than by being right, and below a 10-cell magnitude the ratchets over those
  exemptions cannot see churn at all — that band, not the generator, is where the next defect will sit.*
  The fix wave closed the 10-cell band with exact pins; the ~200 excused rows are the honest remaining limit.

### After merge

- Delete the worktree at `C:/Users/Jouda/RiderProjects/DwarfMapper-surface` and the agent worktree under
  `.claude/worktrees/`. The ledgers are committed under `Issues/ledgers/`; nothing else in `.superpowers/sdd/`
  is needed.
- The nightly mutation leg only runs on the default branch — it activates the moment this lands on `master`.
- Round 21 research (`Issues/round21/RESEARCH.md`) is measured and closed except E4, which is optional.
