<!-- SPDX-License-Identifier: GPL-2.0-only -->

# Round 24 — auditing the generator's own output with static analysis (I21)

Filed 2026-08-23. Closes the measurement half of `Issues/round20/TASKS.md` **I21**; leaves the
always-on-gate half open with a named blocker.

## The premise, and the correction to it

I21's framing is right and the `<PropertyGroup>` in it is **necessary but not sufficient**:

```xml
<EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles>
<CompilerGeneratedFilesOutputPath>$(BaseIntermediateOutputPath)generated</CompilerGeneratedFilesOutputPath>
```

That writes files to disk. It does **not** make analyzers read them. `MapEmitter.cs:14` and
`DwarfGenerator.cs:271` write `// <auto-generated/>` as the first line of every emitted file, which is
exactly what Roslyn's analyzer driver reads to classify a file as generated and skip it. Overriding that
verdict — `generated_code = false` scoped to the emitted path — is the half that does the work. The two
halves are orthogonal.

**Proved by control, not assumed.** With the emission property on and the `.editorconfig` override
absent: `0` warnings across 571 emitted files. With the override added: 1,092. The premise holds.

### Where the property must live

`Directory.Build.props` is imported **before** the SDK defines `BaseIntermediateOutputPath`, so there
`$(BaseIntermediateOutputPath)generated` evaluates to a bare `generated` — a directory in the PROJECT
ROOT, which the SDK's `**/*.cs` glob then compiles as ordinary source. Measured, not theorised: 2,004 ×
CS0111 and 1,084 × CS0757 across the solution. It belongs in `Directory.Build.targets`, imported after
the SDK props. It is now there, Debug-only, so Release stays clean for the reproducible-build and
package-size gates.

## What the sweep measured

571 emitted files, 29,888 lines, **1,092 warnings across 542 distinct sites**.

| Rule | Sites | Disposition |
|---|---:|---|
| CA1822 can be static | 1,066 | **intent** — emitted `Map`/`ToDto` are instance methods because the CONSUMER's partial declares them so |
| CA1859 prefer concrete type | 8 | **intent** — the helper returns the DESTINATION MEMBER's declared type and is assigned straight into it |
| MA0192 use HasFlag | 6 | **intent** — bitwise is the fast path; HasFlag is the readability choice |
| CA2255 ModuleInitializer in a library | 4 | **intent** — module init IS the ambient registry's self-registration |
| CA1852 seal internal type | 4 | **filed** — see below |
| CA1707 no underscores | 2 | **intent** — `__Dwarf` is deliberate collision avoidance |
| CA1508 dead conditional | 2 | **filed** — see below |
| CA1510 prefer ThrowIfNull | 10 | **REAL DEFECT — FIXED** |

That distribution is I21's predicted "triage flood" exactly: 97 % of the findings are one rule firing on
a design decision the generator cannot change.

## The defect it found, and fixed

Two emitters disagreed about one contract:

| Emitter | Emitted null guard |
|---|---|
| `MapEmitter` | `global::System.ArgumentNullException.ThrowIfNull(source);` |
| `MapToGenerator` | `if (source is null) throw new global::System.ArgumentNullException(nameof(source));` |

One product, two idioms, same TFM. Invisible by construction: both compile, so the compiler is silent,
and a snapshot pins whatever it is handed. `MapEmitter` documents why the helper form is right (the
`throw` lives in a `[DoesNotReturn]` helper, so the hot method stays small IL and inlines; an inline
`throw` can block inlining) and that argument applies at least as strongly to `[MapTo]`'s extension
methods.

`MapToGenerator` already carried the comment *"the two sites disagreeing is exactly how this path came to
differ from the nested-helper one"* — it guarded divergence WITHIN itself and never saw the divergence
from `MapEmitter`.

Fixed at both sites. `RegistryValueTypeSourceTests.A_reference_type_source_keeps_its_null_guard` counted
the inline string; its stated intent is "two methods, two guards", so it now counts the helper form AND
asserts the inline form is absent — the previous assertion passed on either idiom, which is why the drift
survived. Golden manifest and five Verify snapshots updated; every delta reviewed and null-guard-only.

## Filed, not forced

**CA1852 — seal the types the generator OWNS.** True for the co-located `[GenerateMap]`-on-a-DTO path,
which invents e.g. `PersonDtoMapper`. False for a declared mapper, which is the consumer's own
`partial class`: one part carrying `sealed` seals the whole type and breaks any consumer deriving from
their own mapper. `MapEmitter` emits one class header (`MapEmitter.cs:36`) for both cases and
`MapperClassModel` carries no flag distinguishing them. Adding one means touching a record that is an
incremental-generator cache key, where equality is load-bearing. Worth doing; not worth doing carelessly.

**CA1508 — the emitted double read.** One site:

```csharp
if (src.Count is not null) dst.Count = src.Count ?? throw new InvalidOperationException("Source member 'Count' was null");
```

`SkipNullSourceMembers` supplies the guard; the nullable→non-nullable coercion supplies the coalesce.
The compiler is right that the fallback is unreachable after the guard — but the two `src.Count` are
separate property invocations, so the coalesce is defence against a source property returning null on the
second read, and throwing there is this library's stated posture. The strictly better emission reads the
member ONCE into a local, removing the dead branch and the double read together. That changes behaviour
in the mutating-property corner (throw → use the first value), so it is a maintainer's call.

## The blocker for an always-on gate

`generated_code = false` works. **Severity scoping inside the same section does not.** With
`dotnet_analyzer_diagnostic.severity = none` plus per-rule `dotnet_diagnostic.CA1822.severity = none` in
`[**/generated/DwarfMapper.Generator/**/*.cs]`, CA1822/CA1508/CA1707/CA1852/CA1859 still fire, while
MA0192 and CA2255 are correctly silenced by the same bulk setting. So the section is matched and partly
honoured — some rules resist it. Under `TreatWarningsAsErrors` that means enabling the override
repo-wide turns 1,066 intent findings into build errors.

Until that is solved, the audit is **on demand, not always-on**, and reproducible:

1. add the `[**/generated/DwarfMapper.Generator/**/*.cs]` section with `generated_code = false`
2. `dotnet build DwarfMapper.NET.sln --no-incremental -p:TreatWarningsAsErrors=false`
3. read diagnostics whose path contains `generated/DwarfMapper.Generator`
4. remove the section

Emission itself stays on in Debug, so the output is always on disk to read and diff — which is the half
that costs nothing and is useful on its own.

## Not yet reached

I21's suggested scope order ends at "eventually point it at K0's generated graph corpus". This sweep
covered samples, consumer projects and the whole test corpus as they stand; the K0 type-graph corpus
generates its mappers inside the compiler-test harness rather than a project, so it emits nothing to
disk and was not analysed.

## Two decisions taken on the findings

### `static` on generator-owned helpers — WORTH DOING, specified here, not improvised

Of the 1,066 CA1822 findings, **360 are on private helpers the generator owns outright** (the `__Dwarf`
prefix: `__DwarfMapColl_*`, `__DwarfMapDict_*`, `__DwarfMap_Obj_*`, `__DwarfMap_Depth_Map`). The other 706
are on consumer-shaped API (`Map` 592, `ToDto` 32, `FromDto`, `Update`, `Replace`, `Patch`) whose
instance-ness is dictated by the consumer's own `partial` declaration and is not the generator's to change.

Those 360 are a real win — no `this` argument, smaller call, better inlining — in code that runs on every
map. It should be done. It must NOT be done by emitting `static` and seeing what breaks, because a wrong
`static` produces code that does not compile IN A CONSUMER'S BUILD, which is the silent-emitted-CS-error
class this repository already hunts (I5, I14, I17).

A helper is static-safe only if nothing in its body binds to an instance, and the emitter currently cannot
know that. Three inputs are missing, all at `MapEmitter.cs:222` / `:230` where `private ` is written:

1. **Hooks.** `MapMethodModel` carries `BeforeHooks`/`AfterHooks` and a synthesized helper replicates the
   pair's hooks, but `HookCall(Name, TakesSource, TargetByRef)` has no `IsStatic`. A user's `[BeforeMap]`
   may be an instance method.
2. **Converters.** `MemberMap.ConverterMethod` is a bare name. `Use = nameof(FormatMoney)` resolves to an
   instance method just as easily as a static one.
3. **Declared-pair reuse.** `NestedMappingRegistry.cs:30` states it: a helper whose element/key/value
   resolves to a PUBLIC DECLARED method calls that method — an instance call by construction.

So the work is: carry staticness for (1) and (2) on the models, and a "calls a declared member" bit for
(3); then emit `static` iff all three say so. Both `HookCall` and `MapMethodModel` are incremental-generator
cache keys, so the added members must participate in equality deliberately — the same care
`EquatableArray` exists to enforce. Expect snapshot churn across most generated files, all of it one token.

**Three further blockers, found while attempting a shortcut and recorded so the next attempt does not
spend the same hours.** The tempting move is a conservative predicate over the EXISTING model — "static iff
no hooks and no converter and no predicate and no value-expression" — needing no new state. It does not
hold, for three independent reasons:

* **Mappers can carry injected instance state.** `DiagnosticDescriptors.cs:590` documents mappers with
  CONSTRUCTOR DEPENDENCIES (they are excluded from ambient self-registration for exactly that reason). A
  helper inside one may legitimately reference an injected field, and no member-level flag reveals it.
* **The helper call graph exists only in rendered text.** `SynthesizedMethod` is `(string Name, string Code)`
  and a collection helper calls its element helper as a plain unqualified instance call inside that string
  (`__r[__i] = __DwarfMap_Obj_...(src[__i])`). `static` is a FIXED POINT over that graph — A can be static
  only if everything A calls is static — and the graph is not in the model at all.
* **`IsSynthesized` is a naming convention, not a flag.** It lives in `GeneratedNames` as a prefix rule, so
  `ConverterMethod == null` does NOT imply "calls nothing"; nested and collection dispatch flows through
  the name, not the field.

The consequence is that the work is genuinely the model change described above, plus a call-graph edge on
`SynthesizedMethod`, plus a `HasConstructorDependencies` bit on `MapperClassModel` — and then a fixed-point
pass. It is a real feature with a real design, not a cleanup, and the failure mode of getting it wrong is
code that does not compile in a CONSUMER's build.

The honest test that this landed correctly is not "the build is green" — it is a fixture per input: an
instance hook, an instance converter, and a declared-pair-reuse nested map, each asserting the helper is
NOT static, plus the converse for the clean case.

### `sealed` on generated types — DECIDED: NO

CA1852 fires on 4 sites, all the co-located `[GenerateMap]`-on-a-DTO path (`PersonDtoMapper`,
`ClPersonDtoMapper`). Sealing them is technically safe today and I am still declining it:

* the type is emitted as `partial`, which IS an extension point — a consumer may add their own part, and
  `sealed` on the generated part seals the whole type out from under them;
* Gallery example 15 documents the emitted type BY NAME ("the generator emits the actual mapper into a
  SEPARATE generated type (PersonDtoMapper), reachable via the generated `model.ToPersonDto()` extension"),
  so it is closer to a documented surface than to an implementation detail;
* the payoff is devirtualization on a type normally reached through a static extension method, where the
  call is already direct;
* and doing it safely still needs the `MapperClassModel` flag that distinguishes generator-owned types from
  consumer partials — the same cache-key change, spent on a smaller prize.

Removing a consumer's extension point to win a virtual dispatch that mostly is not happening is a bad
trade. If this is revisited, the argument to beat is the `partial` one, not the perf one.

## Tooling note — Rider's inspector cannot reach emitted code

`lint_files` over the emitted files returns *"File is not included in any project of the current
solution"* for every one of them, and that is correct: they live under `obj/`, which is not a project
item. So the ReSharper rule set — a genuinely different set from the Roslyn analyzers, and the one that
caught the `//`-in-a-cref damage during the reformat repair — has no route to the generated output as
things stand. Reaching it would mean compiling the emitted files as ordinary source in a throwaway audit
project, which for test-corpus output means reproducing each consumer's own types; not obviously worth it.

The Roslyn route (`generated_code = false`) IS the working mechanism and is the one that found the real
defect, so this is a recorded limitation rather than a blocker.

## Regression cover

`SelfValidation/EmittedNullGuardScanTests` scans the generator's SOURCE for emitted null guards and bans
the inline `throw new ArgumentNullException` form. A per-emitter test would not have caught the original
defect — each emitter was self-consistent and the divergence lived BETWEEN them — so the check runs over
the whole generator at once, which also covers a third emitter added later without anyone remembering.

It carries its own anti-vacuity fact: the corpus must be real (>20 sources) and at least TWO files must
still emit the helper form, because the ban only means something while more than one emitter writes a
guard — that plurality is the entire reason they can drift.

Proved to bite: reintroducing the inline form at ONE of `MapToGenerator`'s two sites fails the test with
`MapToGenerator.cs:470`, then reverted.
